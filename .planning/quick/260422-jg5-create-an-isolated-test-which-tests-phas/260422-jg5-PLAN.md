---
quick_task_id: 260422-jg5
 type: execute
 wave: 1
 depends_on: []
 files_modified: [tests/test_04_water_integration.py]
 autonomous: true
 requirements: []
 user_setup: []
 must_haves:
   truths:
     - "Test can query water features from OSM using load_water_features()"
     - "Returned geodataframes contain expected geometry types (polygons for lakes, linestrings for rivers)"
     - "Returned geodataframes are projected to target CRS"
   artifacts:
     - path: "tests/test_04_water_integration.py"
       provides: "Isolated integration test for water feature querying"
       contains: "load_water_features function call"
   key_links:
     - from: "test_04_water_integration.py"
       to: "routing_2026.load_water_features()"
       via: "import routing_2026"
       pattern: "import routing_2026"
---

<objective>
Create an isolated integration test that validates Phase 04 water feature querying functionality by:
- Calling load_water_features() with a realistic Norway bbox
- Verifying returned GeoDataFrames have correct structure and projection
- Validating geometry types (polygons for lakes, linestrings for rivers)

Purpose: Provide focused test coverage for water metadata querying that demonstrates Phase 04 works end-to-end with real OSM data

Output: test_04_water_integration.py with live OSM query test
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@routing_2026.py

# load_water_features function signature
```python
def load_water_features(bbox, target_epsg, timeout=30):
    """
    Query and project water features for water penalty routing.

    Args:
        bbox: Tuple (west, south, east, north) in EPSG:4326 (lat/lon)
        target_epsg: Target EPSG code (e.g., 25832 for UTM 32V)
        timeout: HTTP request timeout in seconds (default: 30)

    Returns:
        Tuple (lakes_gdf, rivers_gdf) - GeoDataFrames projected to target CRS
        Returns (None, None) on network timeout or error
    """
```

# Expected output structure
- lakes_gdf: GeoDataFrame with geometry=Polygon, water bodies
- rivers_gdf: GeoDataFrame with geometry=LineString, waterways
- Both projected to target_epsg CRS

@tests/test_04_01_water_query.py
# Test patterns for water feature testing
- Uses pytest.mark.water marker
- Skip if import not available
- Validate bbox assertions
- Check CRS projection
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create isolated integration test for water feature querying</name>
  <files>tests/test_04_water_integration.py</files>
  <action>
Create test_04_water_integration.py with a single integration test:

1. Import load_water_features from routing_2026
2. Define a realistic bbox in Norway (e.g., Oslo area: (10.5, 59.8, 10.8, 60.0))
3. Define target EPSG (25832 for UTM 32V standard for Norway)
4. Call load_water_features(bbox, target_epsg, timeout=60)
5. Validate:
   - Returns tuple with 2 elements (lakes_gdf, rivers_gdf)
   - If lakes_gdf is not None: 
     * Is a GeoDataFrame
     * Has CRS matching target_epsg
     * Plausible geometry types (Polygon, MultiPolygon)
     * Has some records (log check for presence of water)
   - If rivers_gdf is not None:
     * Is a GeoDataFrame
     * Has CRS matching target_epsg
     * Plausible geometry types (LineString, MultiLineString)
     * Has some records (log check for presence of water)
6. Add @pytest.mark.water marker
7. Import guard: skip if routing_2026 import not available

Key implementation details:
- Usebbox = (10.5, 59.8, 10.8, 60.0) - Oslo area, good source of water features
- Target EPSG = 25832 (UTM 32V, standard for Norway)
- Timeout = 90 seconds to allow slower OSM queries
- Add docstring explaining test purpose and what it validates
- Use descriptive test function name: test_water_features_integration_oslo_area
- Include comments explaining what each assertion validates
</action>
  <verify>
```bash
# Run the test with water marker
pytest tests/test_04_water_integration.py -v -m water --tb=short
```
</verify>
  <done>
Integration test created and passes:
- Successfully queries water features for Oslo bbox
- Validates returned GeoDataFrames have correct structure
- Confirms CRS projection to EPSG:25832
- Logs water feature counts for visibility
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → OpenStreetMap API | External dependency via osmnx library |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-JG5-01 | Spoofing | OSM water data | accept | Test validates structure, not data integrity - spoofing would only affect test validation, not production security |
| T-JG5-02 | Tampering | OSM response | accept | Production code handles network failures gracefully - test accepts both success and None returns |
| T-JG5-03 | Denial of Service | Network timeout | accept | Uses generous timeout (90s) and production code has fallback to None on failure |
</threat_model>

<verification>
All tests pass with water marker:
```bash
pytest tests/test_04_water_integration.py -v -m water --tb=short
```

Test validates:
- Function call succeeds or fails gracefully
- Returned data has correct structure when available
- CRS projection matches target EPSG
- Geometry types are appropriate for water features
</verification>

<success_criteria>
Test file created and test executes:
- [ ] File exists at tests/test_04_water_integration.py
- [ ] Test imports load_water_features from routing_2026
- [ ] Test runs with pytest -m water marker
- [ ] Test validates basic structure of returned data
- [ ] Test has descriptive docstring and comments
</success_criteria>

<output>
After completion, create `260422-jg5-SUMMARY.md` in quick task directory
</output>