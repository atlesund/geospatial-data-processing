---
phase: 04-water-body-penalty-routing
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified: [tests/test_04_04_integration.py]
autonomous: true
requirements: [COMP-01]
user_setup: []
must_haves:
  truths:
    - Dijkstra pathfinding routes avoid water crossings when land alternatives exist
    - Routes cross water only when necessary (e.g., fjord requiring detour)
    - Routes use penalty factors (lakes=10×, rivers=5×, fjords=50×) correctly
    - Pathfinding terminates and respects combined weights
  artifacts:
    - path: tests/test_04_04_integration.py
      provides: Integration tests for water-aware routing
      exports: ['test_route_avoids_lake', 'test_route_avoids_river', 'test_route_accepts_fjord_crossing', 'test_terrain_and_water_combined']
  key_links:
    - from: test_04_04_integration.py
      to: routing_2026.shorest_path()
      via: Integration test calling Dijkstra with water-aware weights
      pattern: 'shortest_path\(|nx.dijkstra_path\('
---

<objective>
Validate water-aware routing through integration testing

Purpose: End-to-end verification that the complete water penalty system (osm queries, crossing detection, combined penalties) produces routes that avoid unnecessary water crossings while finding optimal paths. This validates the entire Phase 4 pipeline from water data retrieval to route computation.

Output: Integration tests demonstrating water-aware routing behavior: routes avoid lakes/rivers, accept fjord crossings with detours, and combine terrain + water penalties correctly
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@routing_2026.py

# Phase Context
@.planning/phases/04-water-body-penalty-routing/04-CONTEXT.md
# Locked Decision D-07: Continue with Dijkstra on combined weights

# Validation Strategy
@.planning/phases/04-water-body-penalty-routing/04-VALIDATION.md
# Per-Task Verification Map: Integration test at task 04

# Testing Patterns
@.planning/codebase/TESTING.md
# Integration test patterns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests for water-aware routing</name>
  <files>tests/test_04_04_integration.py</files>
  <read_first>
    - routing_2026.py (verify shortest_path() function and RoutingNetwork class)
    - tests/test_routing_graph.py (lines 1 - 100 for integration test patterns)
    - tests/test_terrain_penalties.py (lines 150 - 200 for terrain integration tests)
  </read_first>
  <action>
Create tests/test_04_04_integration.py with five integration test functions:

Test 1: test_route_avoids_lake
- Create synthetic routing network: 3x3 grid (9 nodes) with mesh spacing 100
- Add mock lake polygon in center covering middle row nodes
- Create edges with water crossing detection
- Find shortest path from top-left to bottom-right
- Assert path avoids lake crossing (takes land detour around water)
- Compare path length to direct line crosses water (should be longer but avoids 10× penalty)

Test 2: test_route_avoids_river
- Create synthetic routing network: 4x4 grid (16 nodes)
- Add mock river linestring crossing diagonally through grid
- Create edges with water crossing detection
- Find path from one side to other
- Assert path avoids river crossing where possible
- Verify water_penalty_factor = 5.0 indicated on edge attributes

Test 3: test_route_accepts_fjord_crossing_with_detour
- Create synthetic routing network: 5x5 grid with fjord at bottom
- Add mock fjord polygon with name='Sognefjord' covering bottom row
- Find path from top-left to bottom-right (must cross fjord)
- Assert path uses land as far as possible before minimal fjord crossing
- Verify water_type='fjord' and water_penalty_factor = 50.0 on crossing edge

Test 4: test_terrain_and_water_combined
- Create synthetic network with steep slope AND water crossing on same edge
- Mock elevation with 30° slope (terrain_penalty ≈ 2.0)
- Mock lake at same edge (water_penalty = 10.0)
- Assert edge penalty_factor = 20.0 (2.0 × 10.0 multiplicatively)
- Verify route selects alternative edge if available

Test 5: test_dijkstra_convergence_with_water_weights
- Create larger synthetic network (10x10 grid)
- Add multiple water features with different types
- Verify Dijkstra terminates within reasonable time (<5 seconds)
- Verify path is deterministic (same start/end gives same route)
- Verify all edges in path have valid weight > 0

All tests should:
- Mark with @pytest.mark.water for Phase 4 identification
- Mark with @pytest.mark.integration for integration test categorization
- Use synthetic geopandas GeoDataFrames for water features
- Mock Raster.get_elevation_at() for elevation data
- Verify both pathfinding success and penalty factor correctness

Add to tests/conftest.py:
```python
config.addinivalue_line("markers", "integration: Integration tests for end-to-end workflows")
```
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_04_04_integration.py -v -m "integration"</automated>
  </verify>
  <done>
    test_04_04_integration.py exists with:
    - Five integration tests: test_route_avoids_lake, test_route_avoids_river, test_route_accepts_fjord_crossing_with_detour, test_terrain_and_water_combined, test_dijkstra_convergence_with_water_weights
    - All tests marked with @pytest.mark.water and @pytest.mark.integration
    - Tests validate route avoidance of lakes/rivers/crossings
    - Tests validate fjord crossings with detours
    - Tests validate multiplicative combination of terrain + water
    - Tests validate Dijkstra termination and determinism
    - conftest.py updated with integration marker
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test → routing module | Test orchestrates full pipeline simulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-10 | Denial of Service | Infinite pathfinding loop | mitigate | Test 5 validates Dijkstra terminates; Dijkstra algorithm guarantees termination on finite graphs |
| T-4-11 | Information Disclosure | Test data leaks routing patterns | accept | Test uses synthetic mock data only (no real locations, no PII) |

**Note:** T-4-11 is low risk - integration tests use synthetic mock data without real-world geographic locations or user information.
</threat_model>

<verification>
- All five integration tests exist and pass
- Dijkstra pathfinding completes with water-aware weights
- Routes avoid lakes and rivers where alternatives exist
- Fjord crossings occur with appropriate detours
- Terrain and water penalties combine multiplicatively
- Pathfinding is deterministic and terminates
</verification>

<success_criteria>
Integration tests validate that the complete water penalty pipeline (osm query → crossing detection → combined weights → Dijkstra) produces routes that avoid unnecessary water crossings while finding optimal paths. Phase 4 implementation is complete and tested end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/04-water-body-penalty-routing/04-04-SUMMARY.md`
</output>