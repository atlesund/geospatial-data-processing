---
phase: 01-map-interaction-user-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, .planning/phases/01-map-interaction-user-selection/tests/conftest.py]
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-05
user_setup: []

must_haves:
  truths:
    - "pytest test framework is installed and can run test commands"
    - "conftest.py provides Screen fixtures for route selector tests"
    - "test fixtures include mock world files for coordinate transformation tests"
  artifacts:
    - path: "requirements.txt"
      provides: "pytest dependency declaration"
      min_lines: 10
    - path: ".planning/phases/01-map-interaction-user-selection/tests/conftest.py"
      provides: "pytest fixtures and configuration"
      min_lines: 30
  key_links:
    - from: "pytest installation"
      to: "test execution"
      via: "pip install command"
      pattern: "pip install pytest"
---

<objective>
Set up test infrastructure with pytest framework, fixtures for Screen class, mock world files, and test data to support automated testing of Phase 1 functionality.

Purpose: Wave 0 establishes test infrastructure before implementing features. Automated tests validate route selection, pan/zoom navigation, and coordinate display functionality.

Output: pytest installed, conftest.py with Screen fixtures, mock world file fixtures, test data directory
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-map-interaction-user-selection/01-RESEARCH.md
@screen_2026.py
@utilities_2026.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pytest dependency</name>
  <files>requirements.txt</files>
  <read_first>requirements.txt</read_first>
  <action>
    1. Read existing requirements.txt to verify current dependencies (numpy, pyproj, folium, pyshp)
    2. Add pytest to requirements.txt with version constraint (pytest>=8.0.0)
    3. Run pip install pytest to install in current virtual environment
    4. Verify installation with pytest --version command
  </action>
  <verify>
    <automated>pytest --version</automated>
  </verify>
  <done>
    pytest is installed (version >= 8.0.0) and requirements.txt contains pytest dependency
  </done>
  <acceptance_criteria>
    - requirements.txt contains line "pytest>=8.0.0"
    - Command "pytest --version" returns version >= 8.0.0
    - pytest is importable in Python environment
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create conftest.py with Screen fixtures</name>
  <files>.planning/phases/01-map-interaction-user-selection/tests/conftest.py</files>
  <read_first>screen_2026.py</read_first>
  <action>
    1. Create .planning/phases/01-map-interaction-user-selection/tests/ directory if it doesn't exist
    2. Create conftest.py with the following fixtures:
       - screen fixture: Creates Screen instance with default dimensions (800x600)
       - screen_with_world_file fixture: Creates Screen with mock world file set
       - mock_world_file fixture: Returns affine transformation tuple [12.0, 0.0, 0.0, -12.0, 450000.0, 6500000.0] (UTM 32V典型 values)
       - mock_epsg fixture: Returns 4326 for decimal degree testing
    3. Add pytest configuration options (test discovery patterns, markers)
    4. Import geo_2026 at module level for Screen class access
  </action>
  <verify>
    <automated>pytest .planning/phases/01-map-interaction-user-selection/tests/ --collect-only</automated>
  </verify>
  <done>
    conftest.py exists with screen, screen_with_world_file, mock_world_file, mock_epsg fixtures
  </done>
  <acceptance_criteria>
    - File .planning/phases/01-map-interaction-user-selection/tests/conftest.py exists and is importable
    - conftest.py contains "def screen():" fixture definition
    - conftest.py contains "def screen_with_world_file():" fixture definition
    - conftest.py contains "def mock_world_file():" fixture returning affine tuple
    - conftest.py contains "def mock_epsg():" fixture returning 4326
    - pytest can collect fixtures without errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create test data directory structure</name>
  <files>.planning/phases/01-map-interaction-user-selection/tests/</files>
  <read_first>None</read_first>
  <action>
    1. Create test data directory: .planning/phases/01-map-interaction-user-selection/tests/data/
    2. Create mock world file (test_world.pgw) with UTM 32V affine transformation values
    3. Create README.md in tests/ directory documenting test structure and fixture usage
    4. Add .gitkeep to test/data/ to ensure directory is tracked
  </action>
  <verify>
    <automated>ls -la .planning/phases/01-map-interaction-user-selection/tests/data/</automated>
  </verify>
  <done>
    Test data directory structure exists with mock world file and documentation
  </done>
  <acceptance_criteria>
    - Directory .planning/phases/01-map-interaction-user-selection/tests/data/ exists
    - File test_world.pgw exists in test data directory with 6 affine parameters
    - File .gitkeep exists in test data directory
    - File README.md in tests/ documents test structure
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → Production code | Mock data crosses into Screen methods |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Test world file data | accept | Test data only validates logic, not security boundary |
| T-01-02 | Denial of Service | pytest execution | accept | Test runner runs in development environment only |
| T-01-03 | Information Disclosure | Fixture data | accept | Mock data with no sensitive information |

</threat_model>

<verification>
- pytest runs successfully with --version command
- conftest.py fixtures are collected without errors
- test data directory structure exists
- pytest can import geo_2026 and Screen class
</verification>

<success_criteria>
pytest >= 8.0.0 installed and configured, conftest.py with Screen fixtures, test data directory with mock world files
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-interaction-user-selection/01-01-SUMMARY.md`
</output>