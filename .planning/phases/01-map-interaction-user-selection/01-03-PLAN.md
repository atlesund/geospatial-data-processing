---
phase: 01-map-interaction-user-selection
plan: 03
type: execute
wave: 3
depends_on: []
files_modified: [screen_2026.py]
autonomous: true
requirements:
  - MAP-03
  - MAP-04
  - MAP-05
user_setup: []

must_haves:
  truths:
    - "User can pan map by dragging middle mouse button or right-click drag"
    - "User can zoom in/out using mouse wheel or keyboard shortcuts"
    - "Pan/zoom works across platforms (Windows, macOS, Linux)"
    - "Selected coordinates display in decimal degrees format (EPSG:4326)"
    - "Coordinate display updates after each point selection"
  artifacts:
    - path: "screen_2026.py"
      provides: "Pan/zoom navigation and coordinate display methods"
      min_lines: 350
  key_links:
    - from: "_handle_mouse_wheel()"
      to: "_zoom_in()/_zoom_out()"
      via: "event.delta or event.num detection"
      pattern: "_handle_mouse_wheel.*_zoom"
    - from: "screen_to_decimal_degrees()"
      to: "text display on canvas"
      via: "pyproj transformation + screen.draw_text()"
      pattern: "screen_to_decimal_degrees.*draw_text"
---

<objective>
Implement map navigation (pan and zoom) with cross-platform mouse wheel support and coordinate display in decimal degrees format, updating display in real-time when points are selected.

Purpose: MAP-03, MAP-04, and MAP-05 require interactive map navigation and WGS84 coordinate display. This plan adds pan using tkinter canvas scan methods, zoom using canvas scale with mouse wheel cross-platform binding, and coordinate transformation via screen_to_world() + pyproj to EPSG:4326.

Output: Modified screen_2026.py with _start_pan(), _do_pan(), _zoom_in(), _zoom_out(), _handle_mouse_wheel(), screen_to_decimal_degrees(), _update_coordinate_display() methods, plus keyboard/mouse event bindings
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-map-interaction-user-selection/01-RESEARCH.md
@screen_2026.py
@utilities_2026.py
@.planning/phases/01-map-interaction-user-selection/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pan methods using tkinter canvas scan_dragto</name>
  <files>screen_2026.py</files>
  <read_first>screen_2026.py</read_first>
  <action>
    1. Read Screen.__init__ to understand _canvas attribute (line 28-35)
    2. After _stop_route_selection() method, add _start_pan(self, event) method:
       - Call self._canvas.scan_mark(event.x, event.y) to mark pan start position
    3. Add _do_pan(self, event) method:
       - Call self._canvas.scan_dragto(event.x, event.y, gain=1) to continue panning
    4. Follow existing code style (4-space indent, underscore prefix)
    5. Reference Tcl/Tk canvas documentation pattern from RESEARCH.md Pattern 2
  </action>
  <verify>
    <automated>grep -n "scan_mark\|scan_dragto" screen_2026.py</automated>
  </verify>
  <done>
    Pan methods implemented using tkinter canvas scan operations
  </done>
  <acceptance_criteria>
    - screen_2026.py contains "def _start_pan(self, event):" method definition
    - screen_2026.py contains "def _do_pan(self, event):" method definition
    - _start_pan() calls self._canvas.scan_mark(event.x, event.y)
    - _do_pan() calls self._canvas.scan_dragto(event.x, event.y, gain=1)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement zoom methods using tkinter canvas scale</name>
  <files>screen_2026.py</files>
  <read_first>screen_2026.py</read_first>
  <action>
    1. Add _zoom_in(self, event) method:
       - Calculate canvas_x = self._canvas.canvasx(event.x) and canvas_y = self._canvas.canvasy(event.y)
       - Set scale_factor = 1.1 (10% zoom in)
       - Call self._canvas.scale('all', canvas_x, canvas_y, scale_factor, scale_factor)
    2. Add _zoom_out(self, event) method:
       - Calculate canvas_x = self._canvas.canvasx(event.x) and canvas_y = self._canvas.canvasy(event.y)
       - Set scale_factor = 0.9 (10% zoom out)
       - Call self._canvas.scale('all', canvas_x, canvas_y, scale_factor, scale_factor)
    3. Follow pattern from RESEARCH.md Pattern 3 (canvas.scale around focal point)
    4. Add comment: "# Zoom in/out around mouse cursor position" above methods
  </action>
  <verify>
    <automated>grep -A 5 "def _zoom_in\|def _zoom_out" screen_2026.py</automated>
  </verify>
  <done>
    Zoom methods implemented using tkinter canvas scale operations around focal point
  </done>
  <acceptance_criteria>
    - screen_2026.py contains "def _zoom_in(self, event):" method definition
    - screen_2026.py contains "def _zoom_out(self, event):" method definition
    - Both methods convert event coordinates using canvasx/canvasy
    - Both methods call self._canvas.scale('all', canvas_x, canvas_y, scale_factor, scale_factor)
    - scale_factor is 1.1 for zoom_in, 0.9 for zoom_out
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement cross-platform mouse wheel handler</name>
  <files>screen_2026.py</files>
  <read_first>screen_2026.py</read_first>
  <action>
    1. Add _handle_mouse_wheel(self, event) method:
       - Check if event.delta exists (Windows/macOS): delta = event.delta
       - Fallback for Linux: delta = 120 if event.num == 4 else -120
       - If delta > 0: call self._zoom_in(event), else: call self._zoom_out(event)
    2. Use platform-independent pattern from RESEARCH.md Code Examples section
    3. Add comment: "# Cross-platform mouse wheel handler for Windows/macOS/Linux" above method
  </action>
  <verify>
    <automated>grep -A 10 "def _handle_mouse_wheel" screen_2026.py</automated>
  </verify>
  <done>
    Cross-platform mouse wheel event handler implemented with platform-specific delta detection
  </done>
  <acceptance_criteria>
    - screen_2026.py contains "def _handle_mouse_wheel(self, event):" method definition
    - Method checks event.delta for Windows/macOS
    - Method handles event.num == 4 / 5 for Linux
    - Method calls _zoom_in() if delta > 0, else _zoom_out()
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Implement coordinate transformation to decimal degrees</name>
  <files>screen_2026.py</files>
  <read_first>screen_2026.py utilities_2026.py</read_first>
  <action>
    1. Read utilities.screen_to_world() (utilities_2026.py lines 356-363) to understand affine transformation
    2. Add screen_to_decimal_degrees(self, screen_point) method:
       - Validate self._world_file is not None, return None if missing
       - Call world_point = utilities.screen_to_world(screen_point, self._world_file)
       - If self._epsg is None or self._epsg == 4326: return world_point (already decimal degrees)
       - Create pyproj.Transformer.from_crs(pyproj.CRS.from_epsg(self._epsg), pyproj.CRS.from_epsg(4326), always_xy=True)
       - Transform: lon, lat = transformer.transform(*world_point)
       - Return [lon, lat] in decimal degrees
    3. Follow coordinates display pattern from RESEARCH.md Code Examples section
    4. Add import pyproj at top of file if not already imported
  </action>
  <verify>
    <automated>grep -n "def screen_to_decimal_degrees" screen_2026.py</automated>
  </verify>
  <done>
    Coordinate transformation method converts screen coordinates to WGS84 decimal degrees
  </done>
  <acceptance_criteria>
    - screen_2026.py contains "def screen_to_decimal_degrees(self, screen_point):" method definition
    - Method checks self._world_file is not None before transformation
    - Method calls utilities.screen_to_world() for affine transformation
    - Method uses pyproj.Transformer for EPSG → 4326 conversion when needed
    - Method returns [lon, lat] in decimal degrees format
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Implement coordinate display update on point selection</name>
  <files>screen_2026.py</files>
  <read_first>screen_2026.py</read_first>
  <action>
    1. Add _update_coordinate_display(self, point, label) method:
       - Call screen_to_decimal_degrees(point) to get coordinates
       - If coordinates is None: return (no display if world file missing)
       - Delete previous display: self.delete('coord_display')
       - Format message: f"{label}: Lat {lat:.6f}, Lon {lon:.6f}"
       - Draw text: self.draw_text(point, message, colour='white', tag='coord_display')
    2. Modify _select_route_point() method to call _update_coordinate_display():
       - After drawing marker, call self._update_coordinate_display([event.x, event.y], 'Start' or 'End')
    3. Modify _do_pan() method to redisplay coordinates after panning:
       - After scan_dragto(), if self._start_point: self._update_coordinate_display(self._start_point, 'Start')
       - If self._end_point: self._update_coordinate_display(self._end_point, 'End')
    4. Modify _zoom_in() and _zoom_out() methods similarly to redraw after zooming
  </action>
  <verify>
    <automated>grep -n "_update_coordinate_display" screen_2026.py</automated>
  </verify>
  <done>
    Coordinate display updates in real-time on point selection, pan, and zoom
  </done>
  <acceptance_criteria>
    - screen_2026.py contains "def _update_coordinate_display(self, point, label):" method definition
    - Method calls screen_to_decimal_degrees() and formats output as "Lat X.XXXXXX, Lon X.XXXXXX"
    - _select_route_point() calls _update_coordinate_display() after drawing marker
    - _do_pan() calls _update_coordinate_display() for start/end points after panning
    - _zoom_in() and _zoom_out() call _update_coordinate_display() for start/end points after zooming
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Add mouse and keyboard event bindings for pan/zoom</name>
  <files>screen_2026.py</files>
  <read_first>screen_2026.py</read_first>
  <action>
    1. In __init__ method, after shift-F9/F10 bindings, add pan bindings:
       - self._canvas.bind('<Button-2>', self._start_pan)  # Middle mouse start pan
       - self._canvas.bind('<B2-Motion>', self._do_pan)   # Middle mouse drag pan
       - self._canvas.bind('<Button-3>', self._start_pan)  # Right mouse start pan (alternative)
       - self._canvas.bind('<B3-Motion>', self._do_pan)   # Right mouse drag pan (alternative)
    2. Add mouse wheel bindings (cross-platform):
       - self._root.bind('<MouseWheel>', self._handle_mouse_wheel)   # Windows/macOS
       - self._root.bind('<Button-4>', self._handle_mouse_wheel)    # Linux scroll up
       - self._root.bind('<Button-5>', self._handle_mouse_wheel)    # Linux scroll down
    3. Add zoom keyboard shortcuts:
       - self._root.bind('<plus>', self._zoom_in)    # Zoom in with + key
       - self._root.bind('<equal>', self._zoom_in)   # Zoom in with = key (unshifted +)
       - self._root.bind('<minus>', self._zoom_out)  # Zoom out with - key
    4. Add comment: "# Pan and zoom navigation bindings" above new bindings
  </action>
  <verify>
    <automated>grep -n "Button-2\|Button-3\|MouseWheel\|Button-4\|Button-5\|plus\|minus" screen_2026.py</automated>
  </verify>
  <done>
    Mouse and keyboard bindings configured for pan/zoom navigation
  </done>
  <acceptance_criteria>
    - screen_2026.py contains '<Button-2>' binding to _start_pan
    - screen_2026.py contains '<B2-Motion>' binding to _do_pan
    - screen_2026.py contains '<Button-3>' and '<B3-Motion>' bindings for right-click pan
    - screen_2026.py contains '<MouseWheel>', '<Button-4>', '<Button-5>' bindings to _handle_mouse_wheel
    - screen_2026.py contains '<plus>', '<equal>', '<minus>' bindings to _zoom_in/_zoom_out
    - All bindings are defined in __init__ method
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Create integration tests for pan/zoom and coordinate display</name>
  <files>.planning/phases/01-map-interaction-user-selection/tests/test_navigation_and_display.py</files>
  <read_first>.planning/phases/01-map-interaction-user-selection/tests/conftest.py</read_first>
  <action>
    1. Create test file: .planning/phases/01-map-interaction-user-selection/tests/test_navigation_and_display.py
    2. Write test for screen_to_decimal_degrees() (test_screen_to_decimal_degrees):
       - Create Screen with world_file and epsg=4326
       - Transform screen point [100, 200], verify returns coordinates
    3. Write test for coordinate display (test_coordinate_display):
       - Create Screen with world_file and epsg, call _update_coordinate_display([100, 200], 'Start')
       - Verify 'coord_display' tag exists on canvas with correct text format
    4. Write test for pan functionality (test_pan_functionality):
       - Create mock start event with x=50, y=50
       - Create mock drag event with x=150, y=150
       - Call _start_pan(start_event) and _do_pan(drag_event)
       - Verify scan_mark and scan_dragto are called (check canvas state)
    5. Write test for zoom functionality (test_zoom_functionality):
       - Create mock event with x=400, y=300
       - Call _zoom_in(event) and _zoom_in(mock_event_2)
       - Verify canvas scale operation is called with correct factors
    6. Write test for mouse wheel cross-platform handler (test_mouse_wheel_handler):
       - Test with event.delta (Windows/macOS)
       - Test with event.num (Linux, Button-4/Button-5)
    7. Write test for coordinate display after pan/zoom (test_coordinate_display_persistence):
       - Set start point and display coordinate
       - Apply pan operation
       - Verify coordinate still displays with correct position/format
    8. Use fixtures from conftest.py (screen_with_world_file, mock_world_file, mock_epsg)
  </action>
  <verify>
    <automated>pytest .planning/phases/01-map-interaction-user-selection/tests/test_navigation_and_display.py -x</automated>
  </verify>
  <done>
    Integration tests validate pan/zoom navigation and decimal degree coordinate display
  </done>
  <acceptance_criteria>
    - File .planning/phases/01-map-interaction-user-selection/tests/test_navigation_and_display.py exists
    - Test file contains 6 test functions covering all MAP-03, MAP-04, MAP-05 requirements
    - All tests pass when run with pytest
    - Tests verify cross-platform mouse wheel behavior
    - Tests validate coordinate display persistence during pan/zoom
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pan/zoom event → Canvas state | Mouse events modify canvas transform state |
| Coordinate transformation → pyproj | Coordinate values pass through external library |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Mouse coordinates in pan/zoom | mitigate | Validate event.x and event.y are within reasonable bounds before scan/scale operations |
| T-01-08 | Denial of Service | Unbounded zoom factor | mitigate | Limit zoom factor range (e.g., 0.1 to 10.0) in _zoom_in/_zoom_out methods |
| T-01-09 | Information Disclosure | Affine transformation parameters | accept | World file data already accessible via existing _digit_points_to_geojson() |
| T-01-10 | Tampering | Coordinate display formatting | mitigate | Validate coordinates before display (range check: lat -90..90, lon -180..180) |

</threat_model>

<verification>
- Pan methods (_start_pan, _do_pan) implement canvas scan operations
- Zoom methods (_zoom_in, _zoom_out) implement canvas scale around focal point
- Cross-platform mouse wheel handler works on Windows/macOS/Linux
- Coordinate transformation (screen_to_decimal_degrees) produces WGS84 decimal degrees
- Coordinate display updates on point selection, pan, and zoom
- Mouse and keyboard bindings configured for all navigation operations
- Integration tests pass for all MAP-03, MAP-04, MAP-05 scenarios
</verification>

<success_criteria>
Users can pan/zoom map with mouse and keyboard, and selected points display coordinates in decimal degrees format (Lat X.XXXXXX, Lon X.XXXXXX)
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-interaction-user-selection/01-03-SUMMARY.md`
</output>