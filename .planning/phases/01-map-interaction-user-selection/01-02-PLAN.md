---
phase: 01-map-interaction-user-selection
plan: 02
type: execute
wave: 2
depends_on: []
files_modified: [screen_2026.py]
autonomous: true
requirements:
  - MAP-01
  - MAP-02
user_setup: []

must_haves:
  truths:
    - "User can click on map to select start point with visual marker"
    - "User can click on map to select end point with different color marker"
    - "Start and end points are tracked in Screen instance state"
    - "Route selection mode can be started and stopped via keyboard bindings"
  artifacts:
    - path: "screen_2026.py"
      provides: "Route selection methods and state tracking"
      min_lines: 300
  key_links:
    - from: "_start_route_selection()"
      to: "<Button-1> event binding"
      via: "self._root.bind('<Button-1>', self._select_route_point)"
      pattern: "_root.bind.*Button-1.*_select_route_point"
    - from: "screen._start_point and screen._end_point"
      to: "coordinate display"
      via: "stored screen coordinates"
      pattern: "_start_point.*_end_point"
---

<objective>
Extend Screen class with route selection state management for selecting start and end points via mouse clicks, with visual markers (red for start, blue for end) and two-state selection workflow.

Purpose: MAP-01 and MAP-02 require click-based point selection with visual differentiation. This plan builds on existing digitizing patterns (_start_digit_points, _get_point) to create a dedicated route selection interface.

Output: Modified screen_2026.py with route selection methods, state tracking (_start_point, _end_point, _route_stage), visual markers with distinct colors
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-map-interaction-user-selection/01-RESEARCH.md
@screen_2026.py
@.planning/phases/01-map-interaction-user-selection/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add route selection state attributes to Screen.__init__</name>
  <files>screen_2026.py</files>
  <read_first>screen_2026.py</read_first>
  <action>
    1. Read screen_2026.py to understand __init__ method structure (lines 14-66)
    2. After existing self._epsg and self._world_file attributes (line 22), add three new attributes:
       - self._start_point = None (stores [x, y] screen coordinates for route start)
       - self._end_point = None (stores [x, y] screen coordinates for route end)
       - self._route_stage = None (tracks selection stage: 'start' or 'end')
    3. Maintain consistent naming pattern with existing attributes (underscore prefix for private state)
  </action>
  <verify>
    <automated>grep -n "_start_point\|_end_point\|_route_stage" screen_2026.py</automated>
  </verify>
  <done>
    Route selection state attributes initialized in Screen.__init__
  </done>
  <acceptance_criteria>
    - screen_2026.py contains line "self._start_point = None"
    - screen_2026.py contains line "self._end_point = None"
    - screen_2026.py contains line "self._route_stage = None"
    - All three attributes are defined in __init__ method after line 22
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement _select_route_point() method for two-state point selection</name>
  <files>screen_2026.py</files>
  <read_first>screen_2026.py</read_first>
  <action>
    1. Read existing _get_point() method (lines 86-92) to understand point storage pattern
    2. After _get_point() method, add new method _select_route_point(self, event):
       - Determine point stage: if self._route_stage is 'start' → select start point, else 'end' → select end point
       - Delete previous marker: self.delete('selected_start') or self.delete('selected_end')
       - Draw new marker: self.draw_point([event.x, event.y], size=6, colour='red' if start else 'blue', tag='selected_start'/'selected_end')
       - Update state: set self._start_point or self._end_point to [event.x, event.y]
       - Toggle stage: cycle between 'start' and 'end' for next selection
       - Print status message: "Start point selected: [x, y]" or "End point selected: [x, y]"
    3. Follow existing code style (4-space indent, underscore prefix for protected methods)
  </action>
  <verify>
    <automated>grep -A 20 "def _select_route_point" screen_2026.py</automated>
  </verify>
  <done>
    _select_route_point() method implements two-state point selection with visual markers
  </done>
  <acceptance_criteria>
    - screen_2026.py contains "def _select_route_point(self, event):" method definition
    - Method contains logic to check self._route_stage ('start' or 'end')
    - Method calls self.delete() to clear previous marker
    - Method calls self.draw_point() with colour='red' for start, 'blue' for end
    - Method updates self._start_point and self._end_point attributes
    - Method toggles self._route_stage between 'start' and 'end'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement _start_route_selection() and _stop_route_selection() methods</name>
  <files>screen_2026.py</files>
  <read_first>screen_2026.py</read_first>
  <action>
    1. Read existing _start_digit_points() and _stop_digit_points() methods (lines 72-101) for pattern reference
    2. Add _start_route_selection() method:
       - Set self._route_stage = 'start'
       - Bind left mouse button to _select_route_point: self._root.bind('<Button-1>', self._select_route_point)
       - Change cursor to crosshair: self.cursor('tcross')
       - Print message: "Route selection started: click to select start point, then end point"
    3. Add _stop_route_selection() method:
       - Unbind left mouse button: self._root.unbind('<Button-1>')
       - Reset cursor: self.cursor()
       - Set self._route_stage = None
       - Print message: "Route selection stopped"
    4. Follow existing pattern for keyboard binding management
  </action>
  <verify>
    <automated>grep -A 10 "def _start_route_selection" screen_2026.py && grep -A 10 "def _stop_route_selection" screen_2026.py</automated>
  </verify>
  <done>
    Route selection mode can be started and stopped with keyboard bindings
  </done>
  <acceptance_criteria>
    - screen_2026.py contains "def _start_route_selection(self, event):" method definition
    - screen_2026.py contains "def _stop_route_selection(self, event):" method definition
    - _start_route_selection() binds '<Button-1>' to '_select_route_point'
    - _start_route_selection() sets cursor to 'tcross'
    - _stop_route_selection() unbinds '<Button-1>'
    - _stop_route_selection() resets cursor
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add keyboard bindings for route selection mode</name>
  <files>screen_2026.py</files>
  <read_first>screen_2026.py</read_first>
  <action>
    1. Read keyboard binding section in __init__ (lines 50-66)
    2. After F9/F12 bindings (line 66), add new bindings:
       - self._root.bind('<Shift-F9>', self._start_route_selection)  # Start route selection mode
       - self._root.bind('<Shift-F10>', self._stop_route_selection)  # Stop route selection mode
    3. Add comment explaining shortcut: "# Route selection mode bindings" above the new lines
    4. Follow existing keybinding format (F9/F12 pattern)
  </action>
  <verify>
    <automated>grep -n "Shift-F9\|Shift-F10" screen_2026.py</automated>
  </verify>
  <done>
    Keyboard bindings configured for route selection mode (Shift-F9 to start, Shift-F10 to stop)
  </done>
  <acceptance_criteria>
    - screen_2026.py contains "self._root.bind('<Shift-F9>', self._start_route_selection)"
    - screen_2026.py contains "self._root.bind('<Shift-F10>', self._stop_route_selection)"
    - Bindings are defined in __init__ method after line 66
    - Comment "# Route selection mode bindings" appears above the new bindings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Create integration tests for route selection functionality</name>
  <files>.planning/phases/01-map-interaction-user-selection/tests/test_route_selector.py</files>
  <read_first>.planning/phases/01-map-interaction-user-selection/tests/conftest.py</read_first>
  <action>
    1. Create test file: .planning/phases/01-map-interaction-user-selection/tests/test_route_selector.py
    2. Write test for route selection state initialization (test_route_selection_state_init):
       - Create Screen instance
       - Verify _start_point, _end_point, _route_stage are initialized to None
    3. Write test for start point selection (test_select_start_point):
       - Create Screen instance, set _route_stage = 'start'
       - Create mock event with x=100, y=200
       - Call _select_route_point(mock_event)
       - Verify _start_point is [100, 200]
       - Verify marker drawn with tag 'selected_start'
    4. Write test for end point selection (test_select_end_point):
       - Create Screen instance, set _route_stage = 'end' with _start_point already set
       - Create mock event with x=300, y=400
       - Call _select_route_point(mock_event)
       - Verify _end_point is [300, 400]
       - Verify marker drawn with tag 'selected_end'
    5. Write test for selection stage toggling (test_selection_stage_toggle):
       - Start with _route_stage = 'start'
       - Select point, verify stage toggles to 'end'
       - Select another point, verify stage toggles back to 'start'
    6. Use pytest fixtures from conftest.py (screen fixture)
  </action>
  <verify>
    <automated>pytest .planning/phases/01-map-interaction-user-selection/tests/test_route_selector.py -x</automated>
  </verify>
  <done>
    Integration tests validate route selection state machine and marker behavior
  </done>
  <acceptance_criteria>
    - File .planning/phases/01-map-interaction-user-selection/tests/test_route_selector.py exists
    - Test file contains 4 test functions: test_route_selection_state_init, test_select_start_point, test_select_end_point, test_selection_stage_toggle
    - All tests pass when run with pytest
    - Tests use Screen fixture from conftest.py
    - Tests verify state transitions and marker drawing behavior
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mouse click event → Screen state | Untrusted screen coordinates cross into internal state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Screen coordinates in _select_route_point | mitigate | Validate event.x and event.y are within canvas bounds (0 <= x < self._columns, 0 <= y < self._rows) before processing |
| T-01-05 | Denial of Service | Unbounded point storage | mitigate | Limit to 1 start + 1 end point only (already enforced by two-state machine) |
| T-01-06 | Spoofing | Keyboard binding conflicts | accept | Shift-F9/F10 not used by existing functionality, no authentication needed |

</threat_model>

<verification>
- New attributes (_start_point, _end_point, _route_stage) defined in __init__
- _select_route_point() method implements two-state selection with color-coded markers
- _start_route_selection() and _stop_route_selection() methods work correctly
- Keyboard bindings Shift-F9/F10 trigger route selection mode
- Integration tests pass for all route selection scenarios
</verification>

<success_criteria>
Users can select start and end points via mouse clicks with visual markers (red start, blue end) and toggle between selection stages
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-interaction-user-selection/01-02-SUMMARY.md`
</output>