---
phase: 03-steep-terrain-penalty-routing
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_terrain_penalties.py, tests/conftest.py]
autonomous: true
requirements: [COMP-02]
user_setup: []

must_haves:
  truths:
    - "Test file tests/test_terrain_penalties.py exists with test function stubs"
    - "All test stubs reference decisions D-01 through D-06"
    - "conftest.py has terrain marker for Phase 3 tests"
    - "Mock elevation grid fixture available for test data"
  artifacts:
    - path: "tests/test_terrain_penalties.py"
      provides: "Test stubs for terrain penalty unit tests"
      contains: "test_slope_calculation, test_penalty_threshold, test_linear_scaling, test_multiplicative_weight, test_realistic_routing"
    - path: "tests/conftest.py"
      provides: "Pytest fixture and marker setup"
      exports: ["pytest.mark.terrain", "elevation_grid fixture"]
  key_links:
    - from: "Plan 03-01"
      to: "test_slope_calculation"
      via: "Validates D-01/D-02 slope formula"
      pattern: "test_slope_calculation"
    - from: "Plan 03-01"
      to: "test_penalty_threshold"
      via: "Validates D-03/D-04 20° threshold"
      pattern: "test_penalty_threshold"
    - from: "Plan 03-02"
      to: "test_linear_scaling"
      via: "Validates D-05 k=0.2 multiplier"
      pattern: "test_linear_scaling"
    - from: "Plan 03-02"
      to: "test_multiplicative_weight"
      via: "Validates D-06 weight = distance × penalty"
      pattern: "test_multiplicative_weight"
    - from: "Plan 03-04"
      to: "test_realistic_routing"
      via: "Validates COMP-02 integration"
      pattern: "test_realistic_routing"
---

<objective>
Generate test scaffold for Phase 3 terrain penalty functionality.

Purpose: VALIDATION.md requires Wave 0 test infrastructure before execution begins. This plan creates test stubs for all terrain penalty behaviors specified in CONTEXT.md (D-01 through D-06) and COMP-02 requirements. Tests will be implemented by subsequent plans using TDD methodology.

Output: tests/test_terrain_penalties.py file with failing test stubs. tests/conftest.py updated with pytest.mark.terrain marker and elevation_grid fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-steep-terrain-penalty-routing/03-CONTEXT.md
@tests/conftest.py

## Locked Decisions (from 03-CONTEXT.md)
**D-01/D-02**: Slope calculation per mesh edge using elevation differences between nodes, convert to degrees.

**D-03/D-04**: 20° threshold for penalty application.

**D-05**: Linear scaling with k=0.2 multiplier.

**D-06**: Multiplicative weight integration.

## COMP-02 Requirements (from REQUIREMENTS.md)
- COMP-02: System applies fixed penalties for steep terrain to ensure realistic hiking routes

## Project Patterns (from Phase 1-2 SUMMARYs)
- Test naming: test_{function}_{behavior} pattern
- Pytest fixtures in conftest.py
- Markers for test categorization
- Mock classes for testing (e.g., MockPhotoImage from test_terrain_mesh.py)

## Reference Code (from existing test patterns)
Existing tests in test_terrain_mesh.py show:
- MockPhotoImage class pattern for testing without actual raster files
- pytest.mark.terrain marker usage
- Elevation grid usage in terrain mesh tests

Reference from test_terrain_mesh.py lines 20-35:
```python
import pytest

def _MockPhotoImage(width, height):
    """Mock tkinter.PhotoImage for testing."""
    class Mock:
        def width(self):
            return width
        def height(self):
            return height
    return Mock()
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_terrain_penalties.py with test stubs</name>
  <files>tests/test_terrain_penalties.py</files>
  <action>Create new file tests/test_terrain_penalties.py with the exact content:

```python
"""
Unit tests for terrain penalty routing functionality.

Phase 3: Steep Terrain Penalty Routing
Tests validate locked decisions D-01 through D-06 from CONTEXT.md.
"""

import pytest
import numpy as np
import math
from routing_2026 import calculate_terrain_weight


@pytest.mark.terrain
def test_slope_calculation():
    """
    Test slope angle calculation per D-01/D-02.

    Validates:
    - Slope = atan(elevation_diff / edge_length)
    - Slope converted to degrees using math.degrees()
    - Guard clause for edge_length == 0
    """
    # TODO: Implement test when calculate_terrain_weight() exists in Plan 03-02
    pytest.skip("calculate_terrain_weight() not yet implemented")


@pytest.mark.terrain
def test_penalty_threshold():
    """
    Test 20° threshold behavior per D-03/D-04.

    Validates:
    - Penalty_factor = 1.0 for slope <= 20°
    - Penalty_factor > 1.0 for slope > 20°
    """
    # TODO: Implement test when calculate_terrain_weight() exists in Plan 03-02
    pytest.skip("calculate_terrain_weight() not yet implemented")


@pytest.mark.terrain
def test_linear_scaling():
    """
    Test linear penalty scaling per D-05.

    Validates:
    - Penalty factor formula: 1.0 + 0.2 * (slope - 20.0)
    - Known examples: 25° = 2.0×, 35° = 4.0×, 45° = 6.0×
    - Penalty clamped to max 100 (mitigation for T-3-07)
    """
    # TODO: Implement test when calculate_terrain_weight() exists in Plan 03-02
    pytest.skip("calculate_terrain_weight() not yet implemented")


@pytest.mark.terrain
def test_multiplicative_weight():
    """
    Test multiplicative weight integration per D-06.

    Validates:
    - Final weight = edge_length × penalty_factor
    - Penalty applied per edge, not per route
    """
    # TODO: Implement test when calculate_terrain_weight() exists in Plan 03-02
    pytest.skip("calculate_terrain_weight() not yet implemented")


@pytest.mark.terrain
def test_edge_length_validation():
    """
    Test edge length validation and guard clauses.

    Validates:
    - ValueError raised for edge_length <= 0 (T-3-08)
    - Returns (0.0, 0.0, 1.0) for edge_length == 0 (T-3-05)
    """
    # TODO: Implement test when calculate_terrain_weight() exists in Plan 03-02
    pytest.skip("calculate_terrain_weight() not yet implemented")


@pytest.mark.terrain
def test_elevation_validation():
    """
    Test elevation value validation per T-3-06.

    Validates:
    - NaN values rejected with ValueError
    - Infinite values rejected with ValueError
    - math.isfinite() check applied
    """
    # TODO: Implement test when calculate_terrain_weight() exists in Plan 03-02
    pytest.skip("calculate_terrain_weight() not yet implemented")


@pytest.mark.terrain
def test_penalty_clamp():
    """
    Test penalty factor clamping to max 100 per T-3-07.

    Validates:
    - Extreme slopes (e.g., 90°) produce penalty_factor = 100, not infinity
    - Clamp applied after linear scaling calculation
    """
    # TODO: Implement test when calculate_terrain_weight() exists in Plan 03-02
    pytest.skip("calculate_terrain_weight() not yet implemented")


@pytest.mark.terrain
def test_realistic_routing():
    """
    Integration test: Dijkstra routes avoid steep terrain edges.

    Validates COMP-02 requirement:
    - System applies fixed penalties for steep terrain
    - Routes avoid unrealistic vertical climbs when alternatives exist
    - Routes follow natural hiking gradients where possible

    Tests:
    - Scenario 1: Flat vs. steep alternative
    - Scenario 2: All steep terrain (only option)
    - Scenario 3: Slope threshold boundary (20°)
    """
    # TODO: Implement test in Plan 03-04 when terrain_mesh_from_raster() has terrain weights
    pytest.skip("terrain_mesh_from_raster() terrain weights not yet integrated")
```

Use exact pytest.skip() messages shown. Ensure all test stubs have @pytest.mark.terrain decorator. Import routing_2026 module (will fail until function exists, but import is placeholder for TDD RED cycle). Do NOT add any additional tests beyond these 9 stubs.
</action>
  <verify>
    <automated>ls tests/test_terrain_penalties.py && grep -q "test_slope_calculation" tests/test_terrain_penalties.py && grep -q "test_penalty_threshold" tests/test_terrain_penalties.py && grep -q "test_linear_scaling" tests/test_terrain_penalties.py && grep -q "test_multiplicative_weight" tests/test_terrain_penalties.py && grep -q "test_realistic_routing" tests/test_terrain_penalties.py && python3 -m pytest tests/test_terrain_penalties.py -v --collect-only</automated>
  </verify>
  <done>tests/test_terrain_penalties.py exists with 9 test stubs. All tests either skip due to missing implementation or are ready for TDD implementation. Pytest collection succeeds showing all 9 tests.</done>
</task>

<task type="auto">
  <name>Task 2: Update conftest.py with terrain marker and elevation_grid fixture</name>
  <files>tests/conftest.py</files>
  <read_first>tests/conftest.py</read_first>
  <action>In tests/conftest.py, add this exact code at the end of the file:

```python
# Phase 3: Steep Terrain Penalty Routing marker and fixtures

def pytest_configure(config):
    """Register custom pytest markers."""
    config.addinivalue_line(
        "markers", "terrain: Mark test as Phase 3 terrain penalty test"
    )


@pytest.fixture
def elevation_grid():
    """
    Mock elevation grid for terrain penalty testing.

    Returns:
        numpy.ndarray: 4x4 elevation grid in meters representing
                      simple terrain with saddle point for testing
                      slope calculations and routing decisions.
    """
    return np.array([
        [100, 100, 100, 100],  # Row 0: flat top edge
        [100, 150, 150, 100],  # Row 1: shallow climb from left
        [100, 150, 150, 100],  # Row 2: shallow climb from left
        [100, 100, 100, 100],  # Row 3: flat bottom edge
    ])
```

Place this code at the very end of conftest.py after all existing fixtures and configuration. Use exact indentation shown (4 spaces). Do NOT modify any existing content in conftest.py. The elevation grid fixture provides 4x4 grid with center saddle point (150m) surrounded by 100m edges, useful for testing slope calculations and routing decisions.
</action>
  <verify>
    <automated>grep -q "markers, \"terrain: Mark test as Phase 3 terrain penalty test\"" tests/conftest.py && grep -q "def elevation_grid" tests/conftest.py && python3 -c "import pytest; from tests.conftest import elevation_grid; print('elevation_grid fixture importable')"</automated>
  </verify>
  <done>tests/conftest.py updated with pytest.register marker for terrain tests. elevation_grid fixture defined providing 4x4 numpy array with saddle point pattern. Pytest recognizes terrain marker and fixture loads successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Plan 0 creates test infrastructure, no production code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | Plan 0 creates test files only, no security concerns |
</threat_model>

<verification>
## Verification Commands

After all tasks complete:
```bash
# Verify test file exists
ls tests/test_terrain_penalties.py

# Verify all test stubs are defined
python3 -m pytest tests/test_terrain_penalties.py -v --collect-only

# Verify marker and fixture
python3 -m pytest tests/test_terrain_penalties.py -v -m terrain --collect-only

# Run stub tests (should all be skipped)
python3 -m pytest tests/test_terrain_penalties.py -v
```

Expected results:
- 9 tests collected
- All tests marked with @pytest.mark.terrain
- All tests skip with pytest.skip() messages
- Pytest marker configured successfully
- elevation_grid fixture available
</verification>

<success_criteria>
- [ ] tests/test_terrain_penalties.py file created
- [ ] File contains 9 test stubs with @pytest.mark.terrain decorator
- [ ] test_slope_calculation stub present (validates D-01/D-02)
- [ ] test_penalty_threshold stub present (validates D-03/D-04)
- [ ] test_linear_scaling stub present (validates D-05)
- [ ] test_multiplicative_weight stub present (validates D-06)
- [ ] test_edge_length_validation stub present (validates T-3-05/T-3-08)
- [ ] test_elevation_validation stub present (validates T-3-06)
- [ ] test_penalty_clamp stub present (validates T-3-07)
- [ ] test_realistic_routing stub present (validates COMP-02 integration)
- [ ] All stubs use pytest.skip() with descriptive messages
- [ ] tests/conftest.py has pytest_configure with terrain marker
- [ ] tests/conftest.py has elevation_grid fixture returning 4x4 numpy array
- [ ] Pytest collection succeeds for all 9 tests
- [ ] Pytest marker can be used with -m terrain flag
</success_criteria>

<output>
After completion, create `.planning/phases/03-steep-terrain-penalty-routing/03-00-SUMMARY.md`
</output>