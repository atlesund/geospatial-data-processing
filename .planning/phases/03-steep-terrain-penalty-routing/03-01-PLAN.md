---
phase: 03-steep-terrain-penalty-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, raster_2026.py]
autonomous: true
requirements: [COMP-02]
user_setup: []

must_haves:
  truths:
    - "System can load elevation data from PNG raster files using Pillow"
    - "Elevation data is accessible as numpy array for slope calculations"
    - "Raster class stores elevation grid alongside photoimage for terrain analysis"
  artifacts:
    - path: "requirements.txt"
      provides: "Pillow dependency declaration"
      contains: "Pillow"
    - path: "raster_2026.py"
      provides: "Elevation data loading and storage"
      exports: ["_elevation_grid", "get_elevation_at()"]
  key_links:
    - from: "routing_2026.py"
      to: "raster_2026.py"
      via: "raster.get_elevation_at(x, y)"
      pattern: "get_elevation_at\\(.*\\,.*\\)"
    - from: "raster_2026.py"
      to: "requirements.txt"
      via: "Pillow import"
      pattern: "from PIL import"
    - from: "Pillow library"
      to: "raster_2026.py"
      via: "np.array(Image.open(filename))"
      pattern: "Image\\.open"
---

<objective>
Enable raster elevation data access by integrating Pillow (PIL) library for reading PNG pixel values.

Purpose: The current Raster class uses tkinter.PhotoImage which has no pixel reading API. Phase 3 requires elevation values from DTM rasters to calculate terrain slopes. This plan adds Pillow to extract elevation grids while maintaining backward compatibility with existing functionality.

Output: Requirements.txt updated with Pillow dependency. Raster class extended with _elevation_grid numpy array attribute and get_elevation_at(x, y) method for coordinate-to-elevation lookup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-steep-terrain-penalty-routing/03-CONTEXT.md
@.planning/phases/03-steep-terrain-penalty-routing/03-RESEARCH.md
@raster_2026.py

## User Decisions (per 03-CONTEXT.md)
**D-XX (User Decision - Critical Blocker Resolution)**: Use Pillow (PIL) library for elevation data access from raster images. This is a blocking decision that MUST be implemented with exact fidelity — no alternatives, no placeholders.

**Per planning_context**:
> The user has chosen to use **Pillow (PIL)** library for elevation data access from raster images. This is a blocking decision that must be implemented in the plans.

## Project Patterns (from Phase 1-2 SUMMARYs)
- TDD workflow: Create Wave 0 stub tests, implement, verify with pytest
- File naming: {module}_2026.py pattern (raster_2026.py)
- Private attributes: _attribute_name with underscore prefix
- Property pattern: _get_attr, _set_attr, property(fget=_get_attr, fset=_set_attr)
- Testing: pytest 9.0.3, fixtures in conftest.py, conftest.py in tests/ directory

## Existing Code Patterns (from raster_2026.py)
Currently Raster class has:
- __init__(): initializes _filename, _epsg, _photoimage, _world_file
- read_image(): loads tkinter.PhotoImage via utilities.input_file()
- EPSG property: _get_epsg, _set_epsg, property(fget=_get_epsg, fset=_set_epsg)
- Shape property: returns [rows, columns] from _photoimage

Phase 2 summary shows Raster used in terrain_mesh_from_raster() with:
- raster._world_file access for affine transformation
- raster.epsg property for coordinate system
- raster.shape property for grid dimensions
- MockPhotoImage class for testing (wrap height/width methods)

## Integration Points (from routing_2026.py:210-275)
terrain_mesh_from_raster() function expects:
- raster._world_file: affine transformation [pixel_width, row_rotation, col_rotation, pixel_height, x_upper_left, y_upper_left]
- raster.epsg: integer EPSG code
- raster.shape: [rows, columns] tuple

Phase 3 will need:
- raster.get_elevation_at(world_x, world_y): returns elevation value for given world coordinates
- Coordinate-to-pixel mapping using world file transformation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pillow dependency to requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>requirements.txt</read_first>
  <action>Add the exact line "Pillow>=10.0.0" to requirements.txt. Use exact version specification ">=10.0.0" to ensure compatibility with recent Python versions. Place Pillow entry after networkx line to keep related image processing libraries grouped near each other. Do NOT add any other dependencies or modify existing entries.</action>
  <verify>
    <automated>grep -q "Pillow>=10.0.0" requirements.txt && python3 -c "from PIL import Image; print('Pillow importable')" 2>&1 | grep -q "Pillow importable"</automated>
  </verify>
  <done>requirements.txt contains "Pillow>=10.0.0" on its own line. Command "python3 -c 'from PIL import Image'" exits 0 without error after pip install.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Raster.__init__ and read_image to load elevation grid</name>
  <files>raster_2026.py</files>
  <read_first>raster_2026.py</read_first>
  <action>In Raster.__init__() method, add new private attribute "self._elevation_grid = None" after existing _world_file initialization. In read_image() method, after self._photoimage = tkinter.PhotoImage(file=filename) line, add these exact lines:

    # Load elevation grid using Pillow for terrain analysis
    from PIL import Image
    self._elevation_grid = np.array(Image.open(filename))

The import should be INSIDE read_image() method (not at module level) to match existing import patterns from Phase 1-2 where utilities is imported at module level but external libraries are loaded at function scope. Ensure numpy is available: add "import numpy as np" at module level if not already present (verify by checking file first). Do NOT modify any other existing code in read_image().</action>
  <verify>
    <automated>grep -q "self._elevation_grid = None" raster_2026.py && grep -q "from PIL import Image" raster_2026.py && grep -q "np.array(Image.open(filename))" raster_2026.py</automated>
  </verify>
  <done>raster_2026.py contains "self._elevation_grid = None" in __init__. raster_2026.py contains "from PIL import Image" and "self._elevation_grid = np.array(Image.open(filename))" in read_image(). File executes without syntax errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add get_elevation_at() method to Raster class</name>
  <files>raster_2026.py</files>
  <read_first>raster_2026.py</read_first>
  <action>Add new public method get_elevation_at() to Raster class after the existing properties section (after line 42). Use exact implementation:

    def get_elevation_at(self, world_x, world_y):
        """
        Get elevation value at world coordinates (x, y).

        Uses world file affine transformation to convert world coordinates
        to pixel indices, then retrieves elevation from grid.

        Args:
            world_x: X coordinate in the raster's EPSG coordinate system
            world_y: Y coordinate in the raster's EPSG coordinate system

        Returns:
            Elevation value from grid, or None if outside bounds or grid not loaded
        """
        if self._elevation_grid is None or self._world_file is None:
            return None

        pixel_width, row_rotation, col_rotation = self._world_file[0:3]
        pixel_height = self._world_file[3]
        x_upper_left, y_upper_left = self._world_file[4:6]

        # Inverse affine transformation to map world -> pixel
        # Coordinate order from terrain_mesh_from_raster: lines 250-251
        # x = x_upper_left + col * pixel_width + row * col_rotation
        # y = y_upper_left + row * pixel_height + col * row_rotation
        # Solve for col, row:
        col = int((world_x - x_upper_left) // pixel_width)
        row = int((world_y - y_upper_left) // pixel_height)

        rows, cols = self.shape
        if 0 <= row < rows and 0 <= col < cols:
            return float(self._elevation_grid[row, col])
        return None

Match the coordinate transformation math EXACTLY from terrain_mesh_from_raster() lines 250-251. Use integer division (//) for pixel index calculation to match Phase 2 grid generation pattern. Return float() to ensure numeric type. Do NOT modify any other methods.</action>
  <verify>
    <automated>grep -q "def get_elevation_at(self, world_x, world_y):" raster_2026.py && grep -q "col = int((world_x - x_upper_left) // pixel_width)" raster_2026.py && grep -q "row = int((world_y - y_upper_left) // pixel_height)" raster_2026.py</automated>
  </verify>
  <done>raster_2026.py contains get_elevation_at() method with exact signature and implementation. Coordinate transformation matches terrain_mesh_from_raster() pattern. Method handles out-of-bounds coordinates by returning None.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PNG file → PIL.Image.open | Untrusted image data read from file system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | Raster.read_image() | accept | DTMs are public geographic information per Phase 2 threat T-5-02 acceptance; validation not required for v1 scope |
| T-3-02 | Information Disclosure | _elevation_grid array | accept | Terrain elevation data is public (Norway DTM50 is open data); no PII involved |
| T-3-03 | Denial of Service | Large PNG files causing memory exhaustion | accept | Evaluate memory limits in v1 MVP; graceful degradation acceptable |
| T-3-04 | Spoofing | Corrupted PNG files causing crashes | mitigate | Wrap Image.open() in try-except, return None on error, print warning via utilities.warning() |
</threat_model>

<verification>
## Verification Commands

After all tasks complete:
```bash
# Verify Pillow installation
python3 -c "from PIL import Image; print('Pillow importable')"

# Verify Raster class elevation loading
python3 -c "from raster_2026 import Raster; import numpy as np; r = Raster(); assert hasattr(r, '_elevation_grid'); print('Elevation grid attribute exists')"

# Verify get_elevation_at method exists
python3 -c "from raster_2026 import Raster; r = Raster(); assert hasattr(r, 'get_elevation_at'); print('get_elevation_at method exists')"

# Run any existing raster tests (if present)
find tests -name "*raster*" -o -name "*terrain*" | head -5 | xargs -I {} python3 -m pytest {} -x -v 2>/dev/null || echo "No existing raster/terrain tests found"
```
</verification>

<success_criteria>
- [ ] requirements.txt updated with Pillow>=10.0.0 dependency
- [ ] Pillow library installs and imports successfully
- [ ] Raster class has _elevation_grid private attribute initialized in __init__
- [ ] Raster.read_image() loads elevation grid using Pillow
- [ ] get_elevation_at(world_x, world_y) method implemented with correct affine transformation
- [ ] get_elevation_at() returns float elevation for valid coordinates
- [ ] get_elevation_at() returns None for out-of-bounds coordinates
- [ ] Coordinate transformation matches terrain_mesh_from_raster() pattern
- [ ] Code executes without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-steep-terrain-penalty-routing/03-01-SUMMARY.md`
</output>