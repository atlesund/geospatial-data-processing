---
phase: 03-steep-terrain-penalty-routing
plan: 02
type: tdd
wave: 1
depends_on: [03-01]
files_modified: [routing_2026.py]
autonomous: true
requirements: [COMP-02]
user_setup: []

must_haves:
  truths:
    - "System calculates slope angle from elevation differences between nodes"
    - "System applies 20° threshold: penalty only applies when slope > 20°"
    - "Penalty factor scales linearly: 1.0 for ≤20°, then 1.0 + 0.2×(slope - 20°)"
    - "Edge weight computed multiplicatively: distance × penalty_factor"
  artifacts:
    - path: "routing_2026.py"
      provides: "Terrain weight calculation with slope-based penalties"
      exports: ["calculate_terrain_weight()"]
  key_links:
    - from: "terrain_mesh_from_raster()"
      to: "calculate_terrain_weight()"
      via: "Function call during edge creation"
      pattern: "calculate_terrain_weight\\(.*elev1.*elev2.*\\)"
    - from: "calculate_terrain_weight()"
      to: "D-02 decision"
      via: "slope_angle = atan(elevation_diff / edge_length)"
      pattern: "math\\.atan\\(elevation_diff / edge_length\\)"
    - from: "calculate_terrain_weight()"
      to: "D-05 decision"
      via: "penalty_factor = 1.0 + 0.2 × (slope - threshold)"
      pattern: "penalty_factor = 1\\.0.*slope_multiplier.*\\(slope_degrees - threshold_degrees\\)"
    - from: "calculate_terrain_weight()"
      to: "D-06 decision"
      via: "return edge_length × penalty_factor"
      pattern: "return edge_length \\* penalty_factor"
---

<objective>
Implement terrain-aware edge weight calculation with slope-based penalties per locked decisions D-01 through D-06.

Purpose: Phase 2 created terrain mesh with uniform edge weights. Phase 3 replaces placeholder weights with terrain-aware calculations that account for steepness. This plan implements the slope calculation logic and penalty function using TDD methodology to ensure correctness before integration.

Output: calculate_terrain_weight() function in routing_2026.py with slope angle calculation, 20° threshold detection, linear penalty scaling, and multiplicative weight integration. Comprehensive unit tests validating all locked decisions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-steep-terrain-penalty-routing/03-CONTEXT.md
@.planning/phases/03-steep-terrain-penalty-routing/03-RESEARCH.md
@routing_2026.py
@raster_2026.py

## Locked Decisions (from 03-CONTEXT.md)
**D-01**: Use slope = elevation_diff / raster_pixel_spacing. Compute slope directly from elevation differences between mesh nodes.

**D-02**: Slope calculation applies per mesh edge. For each edge, compute elevation difference between two endpoint nodes, divide by edge length (matches mesh_spacing), convert to degrees: slope_angle = atan(elevation_diff / horizontal_distance).

**D-03**: Penalty applies when slope > 20 degrees. Below or equal to 20°, no penalty.

**D-04**: 20° chosen as aggressive threshold. Allows moderately steep terrain while avoiding unrealistic vertical climbs.

**D-05**: Linear scaling. Penalty_factor = 1.0 for slope ≤ 20°. For slope > 20°, penalty_factor = 1.0 + k × (slope - 20°) where k = 0.2. Creates smooth gradients:
  - 25° slope: penalty_factor = 2.0 (2× harder)
  - 35° slope: penalty_factor = 4.0 (4× harder)
  - 45° slope: penalty_factor = 6.0 (6× harder)

**D-06**: Multiplicative. Final weight = distance × penalty_factor. Applies per edge, not per whole route.

**D-07**: Continue with Dijkstra on updated weights (Plan 04 implementation).

## Project Patterns (from Phase 1-2 SUMMARYs)
- TDD workflow: RED (write failing test) → GREEN (implement minimal code) → REFACTOR (clean up)
- Each TDD cycle produces atomic commits with conventional commits
- Test naming: test_{function}_{behavior} pattern
- Function parameters: required params first, optional params with defaults
- Use math module for trigonometric functions (verified available)

## Existing Code (from routing_2026.py)
Current terrain_mesh_from_raster() function (lines 210-275):
- Creates regular grid of nodes
- Connects adjacent nodes with uniform edge_weight = mesh_spacing
- Uses world file for coordinate transformation
- Stores edges with weight, length, source='terrain' attributes

Plan 02 will call calculate_terrain_weight() during edge creation (lines 257-270) to replace uniform weights.

## Integration Points (from 03-RESEARCH.md Research - Standard Stack)
- math.atan() and math.degrees() verified available in Python 3 stdlib
- numpy operations available for any array needs
- Phase 02 (this plan) provides function signature that Plan 03 will integrate

## Dependencies
- Plan 03-01: Elevation grid available via raster.get_elevation_at(x, y)
- Plan 03-03: Will integrate calculate_terrain_weight() into terrain_mesh_from_raster()
</context>

<feature>
  <name>Terrain Weight Calculation with Slope Penalties</name>
  <files>routing_2026.py, tests/test_terrain_penalties.py</files>
  <behavior>
Calculate terrain-aware edge weight with steep terrain penalty:

    **Slope Calculation (D-01, D-02)**:
    - Input: elev1 (float), elev2 (float), edge_length (float > 0)
    - Elevation difference: abs(elev2 - elev1)
    - Slope radians: atan(elevation_diff / edge_length)
    - Slope degrees: math.degrees(slope_radians)
    - Guard clause: if edge_length == 0, return (0.0, 0.0, 1.0)

    **20° Threshold (D-03, D-04)**:
    - If slope_degrees <= 20.0: penalty_factor = 1.0
    - If slope_degrees > 20.0: penalty_factor = 1.0 + 0.2 × (slope_degrees - 20.0)

    **Linear Scaling Examples (D-05)**:
    - 0° slope: penalty = 1.0 ×
    - 20° slope: penalty = 1.0 ×
    - 25° slope: penalty = 2.0 ×
    - 35° slope: penalty = 4.0 ×
    - 45° slope: penalty = 6.0 ×

    **Multiplicative Weight (D-06)**:
    - Final weight: edge_length × penalty_factor
    - Return tuple: (weight, slope_degrees, penalty_factor) for traceability

    **Parameters (per D decision defaults)**:
    - threshold_degrees = 20.0 (D-03/D-04)
    - slope_multiplier = 0.2 (D-05)
  </behavior>
  <implementation>
Function signature (public module-level function):

    def calculate_terrain_weight(elev1, elev2, edge_length,
                                 threshold_degrees=20.0, slope_multiplier=0.2):
        """
        Calculate terrain-aware edge weight with steep terrain penalty.

        Per CONTEXT.md D-01 through D-06:
        - D-01/D-02: Slope from elevation difference, convert to degrees
        - D-03/D-04: 20° threshold for penalty application
        - D-05: Linear scaling with k=0.2 multiplier
        - D-06: Multiplicative weight integration

        Args:
            elev1: Elevation at first node (meters)
            elev2: Elevation at second node (meters)
            edge_length: Distance between nodes in meters (must be > 0)
            threshold_degrees: Slope threshold for penalty (default 20.0°)
            slope_multiplier: Linear scaling factor (default 0.2)

        Returns:
            Tuple (weight, slope_degrees, penalty_factor):
            - weight: Edge weight = edge_length × penalty_factor
            - slope_degrees: Calculated slope angle in degrees
            - penalty_factor: Applied penalty (1.0 for ≤20°, higher for steeper)

        Raises:
            ValueError: If edge_length <= 0
        """
Place this function after load_osmnx_trails() function and before terrain_mesh_from_raster() in routing_2026.py (around line 210, before existing terrain_mesh_from_raster() function). Use math.atan() and math.degrees() for trigonometric calculations. Validate edge_length > 0 with ValueError per D-02 assumption that edge_length is horizontal distance basis.

Return type is tuple (weight, slope_degrees, penalty_factor) to enable Plan 03 to add slope_angle and penalty_factor attributes to edges for traceability.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elevation parameters → calculate_terrain_weight | Numeric input from raster data (trusted source: public DTM) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-05 | Denial of Service | Division by zero in slope calculation | mitigate | Guard clause: if edge_length == 0 return (0.0, 0.0, 1.0) before atan division |
| T-3-06 | Tampering | Invalid elevation values (NaN, infinite) | mitigate | Validate elev1 and elev2 are finite numbers: use math.isfinite() check, raise ValueError with clear message |
| T-3-07 | Denial of Service | Extreme penalty factors causing astronomical weights | mitigate | Clamp penalty_factor to max 100 in function: penalty_factor = min(100, penalty_factor) after calculation |
| T-3-08 | Spoofing | Negative edge_length causing undefined behavior | mitigate | Validate edge_length > 0 with ValueError: if edge_length <= 0: raise ValueError("edge_length must be positive") |
</threat_model>

<verification>
## Verification Commands

After each TDD cycle:
```bash
# Run current test(s) in development
python3 -m pytest tests/test_terrain_penalties.py -x -v -k "test_slope_calculation"

# After all tasks complete, run full test suite
python3 -m pytest tests/test_terrain_penalties.py -x -v
```

Expected test coverage:
- test_slope_calculation: Validates D-01/D-02 slope formula
- test_penalty_threshold: Validates D-03/D-04 20° threshold
- test_linear_scaling: Validates D-05 k=0.2 multiplier with known examples
- test_multiplicative_weight: Validates D-06 weight = distance × penalty
- test_edge_length_validation: Validates T-3-05/T-3-08 guard clauses
- test_elevation_validation: Validates T-3-06 NaN/infinite rejection
- test_penalty_clamp: Validates T-3-07 max 100 clamp
</verification>

<success_criteria>
- [ ] calculate_terrain_weight() function implemented in routing_2026.py
- [ ] Function handles edge_length == 0 gracefully (returns 0.0 weight)
- [ ] Function validates edge_length > 0 with ValueError
- [ ] Function validates elevation values are finite (math.isfinite)
- [ ] Slope calculation uses atan(elevation_diff / edge_length)
- [ ] Slope converted to degrees using math.degrees()
- [ ] Penalty threshold of 20° applied correctly
- [ ] Linear scaling with k=0.2 multiplier yields correct values for 25°, 35°, 45°
- [ ] Multiplicative weight calculation: edge_length × penalty_factor
- [ ] Penalty factor clamped to max 100
- [ ] All 7 unit tests in test_terrain_penalties.py pass
- [ ] Function placed before terrain_mesh_from_raster() in routing_2026.py
- [ ] Code follows project conventions (lowercase_with_underscores, docstrings)
</success_criteria>

<output>
After completion, create `.planning/phases/03-steep-terrain-penalty-routing/03-02-SUMMARY.md`
</output>