---
phase: 02-routing-network-construction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, .planning/phases/02-routing-network-construction/tests/conftest.py]
autonomous: true
requirements: [COMP-03, COMP-04, COMP-05]
user_setup: []

must_haves:
  truths:
    - networkx library is installed and importable
    - scipy library is installed and importable
    - osmnx library is installed and importable
    - Test fixtures exist for RoutingNetwork testing
    - Mock data fixtures available for OSM and trail testing
  artifacts:
    - path: "requirements.txt"
      provides: "Dependency declaration for networkx, scipy, osmnx"
      contains: "networkx>=3.6.1"
    - path: ".planning/phases/02-routing-network-construction/tests/conftest.py"
      provides: "Pytest fixtures for routing network tests"
      exports: ["mock_routing_network", "mock_osm_graph", "mock_trail_vector"]
    - path: ".planning/phases/02-routing-network-construction/tests/data/.gitkeep"
      provides: "Test data directory placeholder"
  key_links:
    - from: "requirements.txt"
      to: "pip install"
      via: "pip install -r requirements.txt"
      pattern: "networkx|scipy|osmnx"
    - from: "tests/conftest.py"
      to: "test imports"
      via: "pytest fixture collection"
      pattern: "def mock_routing_network"
---

<objective>
Set up test infrastructure and install routing dependencies for Phase 2 network construction.

Purpose: Establish the foundation for all Phase 2 plans by installing required dependencies (networkx, scipy, osmnx) and creating test fixtures that support graph construction, OSM integration, and trail conversion testing.

Output: Updated requirements.txt, pytest fixtures in conftest.py, test data directory structure
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-routing-network-construction/02-RESEARCH.md

# Dependencies from Phase 1 test infrastructure
@.planning/phases/01-map-interaction-user-selection/tests/conftest.py
</context>

<interfaces>
<!-- No prior plan interfaces to reference. This is Wave 1, establishing foundation. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install networkx, scipy, and osmnx dependencies</name>
  <files>requirements.txt</files>
  <read_first>requirements.txt</read_first>
  <action>
    Add the following dependencies to requirements.txt (one per line):
    - networkx>=3.6.1
    - scipy>=1.17.1
    - osmnx>=2.1.0

    After updating requirements.txt, install the packages using pip:
    ```bash
    python -m pip install networkx>=3.6.1 scipy>=1.17.1 osmnx>=2.1.0
    ```

    Verify installations by checking each package can be imported:
    - networkx: `python -c "import networkx as nx; print(nx.__version__)"`
    - scipy: `python -c "import scipy; print(scipy.__version__)"`
    - osmnx: `python -c "import osmnx as ox; print(ox.__version__)"`

    All imports should succeed without errors.
  </action>
  <verify>
    <automated>python -c "import networkx, scipy, osmnx; print('All imports successful')"</automated>
  </verify>
  <done>
    - requirements.txt contains networkx>=3.6.1, scipy>=1.17.1, osmnx>=2.1.0
    - networkx imports successfully
    - scipy imports successfully
    - osmnx imports successfully
  </done>
  <acceptance_criteria>
    - requirements.txt contains "networkx>=3.6.1"
    - requirements.txt contains "scipy>=1.17.1"
    - requirements.txt contains "osmnx>=2.1.0"
    - Command `python -c "import networkx"` exits 0
    - Command `python -c "import scipy"` exits 0
    - Command `python -c "import osmnx"` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create test fixtures for routing network testing</name>
  <files>.planning/phases/02-routing-network-construction/tests/conftest.py</files>
  <read_first>
    requirements.txt
    .planning/phases/01-map-interaction-user-selection/tests/conftest.py
  </read_first>
  <action>
    Create a new conftest.py file in the Phase 2 tests directory with the following fixtures:

    1. **Path setup**: Add project root to sys.path (same pattern as Phase 1 conftest)

    2. **Imports**: Import geo_2026, networkx, scipy, osmnx

    3. **pytest_configure()**: Add pytest markers:
       - routing: Tests for routing network construction
       - osmnx: Tests for OSM data integration
       - trails: Tests for trail polyline conversion
       - terrain: Tests for terrain mesh generation

    4. **mock_routing_network()** fixture:
       - Creates a simple networkx.Graph with 4 nodes and 4 edges
       - Nodes: coordinates in UTM 32V (e.g., (450000, 6500000), (450100, 6500100), etc.)
       - Edges: bidirectional with weight=100 (100m segments)
       - Returns: plain dict with {'graph': G, 'epsg': 25832, 'node_coords':coords}

    5. **mock_osm_graph()** fixture:
       - Creates mock osmnx-like MultiDiGraph structure
       - Nodes: integer IDs with x/y attributes in projected coords
       - Edges: with length and highway attributes
       - Returns: MultiDiGraph with ~5 nodes, ~6 edges

    6. **mock_trail_vector()** fixture:
       - Creates a geo.Vector instance with POLYLINE geometry
       - Adds 2-3 trail polylines with coordinates forming a connected network
       - Sets EPSG to 25832
       - Returns: Vector instance ready for graph conversion

    7. **mock_world_file()** (reuse from Phase 1):
       - Returns affine transformation [12.0, 0.0, 0.0, -12.0, 450000.0, 6500000.0]

    Follow the same pattern as Phase 1 conftest.py for consistency and path resolution.
  </action>
  <verify>
    <automated>cd /Users/dev/Code/School/geospatial-data-processing && python -c "import sys; sys.path.insert(0, '.planning/phases/02-routing-network-construction/tests'); import conftest; print('conftest imports successfully')"</automated>
  </verify>
  <done>
    - conftest.py exists with 6 fixtures
    - mock_routing_network returns graph with 4+ nodes
    - mock_osm_graph returns MultiDiGraph with x/y node attributes
    - mock_trail_vector returns Vector with POLYLINE geometry
    - pytest markers registered (routing, osmnx, trails, terrain)
  </done>
  <acceptance_criteria>
    - File ".planning/phases/02-routing-network-construction/tests/conftest.py" exists
    - conftest.py contains "def mock_routing_network"
    - conftest.py contains "def mock_osm_graph"
    - conftest.py contains "def mock_trail_vector"
    - conftest.py contains "config.addinivalue_line" with marker "routing"
    - Command: `python -c "from .planning.phases.02-routing-network-construction.tests.conftest import mock_routing_network"` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create test data directory structure</name>
  <files>.planning/phases/02-routing-network-construction/tests/data/.gitkeep</files>
  <read_first>
    requirements.txt
    .planning/phases/01-map-interaction-user-selection/tests/conftest.py
  </read_first>
  <action>
    Create the test data directory structure for Phase 2:

    1. Create directory: `.planning/phases/02-routing-network-construction/tests/data/`

    2. Create `.gitkeep` file to track directory in git (empty file)

    3. Create `README.md` in tests/ directory documenting:
       - Purpose of test fixtures (mock_routing_network, mock_osm_graph, mock_trail_vector)
       - Structure of mock data (coordinate systems, edge weights)
       - Usage examples for each fixture
       - Notes on running specific test subsets: `pytest -m routing` or `pytest -m osmnx`
  </action>
  <verify>
    <automated>test -f /Users/dev/Code/School/geospatial-data-processing/.planning/phases/02-routing-network-construction/tests/data/.gitkeep && echo "Directory exists"</automated>
  </verify>
  <done>
    - tests/data/ directory exists with .gitkeep
    - tests/README.md documents fixtures and usage
    - Test infrastructure ready for subsequent plans
  </done>
  <acceptance_criteria>
    - File ".planning/phases/02-routing-network-construction/tests/data/.gitkeep" exists
    - File ".planning/phases/02-routing-network-construction/tests/README.md" exists
    - README.md contains "mock_routing_network" reference
    - README.md contains "pytest -m routing" usage example
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install → Python environment | Package installation may download third-party code |
| Conftest fixtures → Test code | Mock data injected into test execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | Package installation (networkx, scipy, osmnx) | accept | Minor risk: packages are well-maintained, verified on PyPI. Version pinning in requirements.txt provides stability |
| T-2-02 | Denial of Service | Test execution with large mock graphs | mitigate | Limit mock graph sizes to 4-6 nodes, use timeout in pytest config if needed |
| T-2-03 | Information Disclosure | Test data in conftest | accept | No sensitive information in test fixtures. Mock coordinates are arbitrary values |
</threat_model>

<verification>
Phase verification:
- pytest can import conftest.py without errors: `pytest .planning/phases/02-routing-network-construction/tests/ --collect-only`
- Dummy test can use mock_routing_network fixture: `pytest .planning/phases/02-routing-network-construction/tests/ -k test_dummy --fixtures -v`
</verification>

<success_criteria>
All dependencies installed and test infrastructure ready:
- requirements.txt updated with networkx, scipy, osmnx
- All three packages import successfully
- conftest.py exists with 6 fixtures
- pytest markers registered for routing tests
- test data directory created with README
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing-network-construction/02-01-SUMMARY.md`
</output>