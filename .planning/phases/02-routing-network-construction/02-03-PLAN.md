---
phase: 02-routing-network-construction
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: [routing_2026.py]
autonomous: true
requirements: [COMP-04]
user_setup: []

must_haves:
  truths:
    - osmnx can load hiking trails from OpenStreetMap within a bounding box
    - load_osmnx_trails function filters for highway types: path, footway, track, steps
    - OSM graph is projected to metric coordinate system (EPSG 25832 for UTM 32V)
    - OSM nodes and edges are converted to RoutingNetwork format
    - Node IDs from OSM are preserved in RoutingNetwork (no ID collisions with other sources)
  artifacts:
    - path: "routing_2026.py"
      provides: "OSM data loading and conversion function"
      exports: ["load_osmnx_trails"]
      min_lines: 80
  key_links:
    - from: "load_osmnx_trails"
      to: "osmnx.graph_from_bbox"
      via: "ox.graph_from_bbox()"
      pattern: "ox\\.graph_from_bbox"
    - from: "osmnx graph"
      to: "RoutingNetwork"
      via: "node iteration, edge iteration, add_node, add_edge"
      pattern: "routing_net\\.add_node|routing_net\\.add_edge"
    - from: "custom_filter"
      to: "highway types"
      via: '\"highway"~"path|footway|track|steps"\' (osmnx filter syntax)
      pattern: 'custom_filter.*path|footway|track|steps'
---

<objective>
Integrate OpenStreetMap hiking trails into routing network using osmnx library.

Purpose: Load hiking-compatible highway types (path, footway, track, steps) from OSM within a user-provided bounding box, project the graph to UTM 32V (EPSG:25832), and convert to RoutingNetwork format for routing operations. Addresses COMP-04 requirement.

Output: load_osmnx_trails function in routing_2026.py that converts OSM data to RoutingNetwork
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-routing-network-construction/02-RESEARCH.md

# Existing classes and interfaces
@routing_2026.py  # (created in plan 02-02)
@.planning/phases/02-routing-network-construction/02-02-SUMMARY.md
</context>

<context>
# Key interfaces from plan 02-02

From routing_2026.py (RoutingNetwork class):
```python
class RoutingNetwork:
    def __init__(self):
        self.graph = nx.Graph()
        self.node_coords = {}
        self.epsg = None

    def add_node(self, node_id, x, y):
        """Add a georeferenced node."""
        self.graph.add_node(node_id)
        self.node_coords[node_id] = (x, y)

    def add_edge(self, u, v, weight, **attrs):
        """Add a weighted edge between two nodes."""
        self.graph.add_edge(u, v, weight=weight, **attrs)

    def shortest_path(self, source, target):
        """Compute shortest path using Dijkstra."""
        return nx.dijkstra_path(self.graph, source, target, weight='weight')

    def find_nearest_node(self, x, y, k=1):
        """Find k nearest nodes using scipy KD-tree."""
        coords_array = np.array(list(self.node_coords.values()))
        tree = scipy.spatial.KDTree(coords_array)
        dist, idx = tree.query([x, y], k=k)
        node_ids = list(self.node_coords.keys())[idx]
        return node_ids[0], dist
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement load_osmnx_trails function</name>
  <files>routing_2026.py</files>
  <read_first>
    routing_2026.py
    .planning/phases/02-routing-network-construction/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: load_osmnx_trails calls ox.graph_from_bbox with correct custom_filter
    - Test 2: load_osmnx_trails projects graph to EPSG specified in epsg parameter (25832) using ox.project_graph
    - Test 3: load_osmnx_trails extracts all nodes from osmnx graph and adds to RoutingNetwork with x,y coordinates
    - Test 4: load_osmnx_trails extracts all edges from osmnx graph and adds as bidirectional edges with length weight
    - Test 5: load_osmnx_trails sets routing_net.epsg to target EPSG code
  </behavior>
  <action>
    Add load_osmnx_trails function to routing_2026.py:

    **Import required module**: import osmnx as ox

    **Function signature**:
    def load_osmnx_trails(bbox, epsg=25832) -> RoutingNetwork:
        """Load hiking trails from OpenStreetMap within bounding box.

        Args:
            bbox: Tuple (south, west, north, east) in decimal degrees (WGS84)
            epsg: Target EPSG for metric coordinate system (default: 25832 = UTM 32V)

        Returns:
            RoutingNetwork instance with OSM trails as graph

        OSM highway types for hiking:
            - 'path'           # Generic path
            - 'footway'        # Pedestrian path
            - 'track'          # Agricultural/forestry road
            - 'steps'          # Stairs
        """
        # Create RoutingNetwork instance
        routing_net = RoutingNetwork()
        routing_net.epsg = epsg

        # Extract bbox bounds
        south, west, north, east = bbox

        # Create osmnx graph filtered for hiking trails
        # [RESEARCH NOTE - see Pattern 3 in 02-RESEARCH.md for osmnx API]
        G_osm = ox.graph_from_bbox(
            north, south, east, west,
            network_type='walk',
            custom_filter='["highway"~"path|footway|track|steps"]'
        )

        # Project to metric coordinate system
        G_osm = ox.project_graph(G_osm, to_crs=f"EPSG:{epsg}")

        # Extract nodes with coordinates
        for node_id, node_data in G_osm.nodes(data=True):
            x = node_data['x']
            y = node_data['y']
            routing_net.add_node(node_id, x, y)

        # Extract edges with weights (bidirectional for hiking)
        for u, v, edge_data in G_osm.edges(data=True):
            weight = edge_data.get('length', 1.0)  # OSM provides length in meters
            routing_net.add_edge(u, v, weight,
                               length=weight,
                               source='osm',
                               highway=edge_data.get('highway', 'unknown'))

        return routing_net

    **Implementation notes**:
    - Follow the exact pattern from RESEARCH.md Pattern 3
    - Use node_data['x'] and node_data['y'] for projected coordinates
    - Store additional edge attributes (source='osm', highway type)
    - Handle missing 'length' attribute gracefully with default 1.0
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_osmnx_integration.py::test_load_osmnx_trails_mock -x -v</automated>
  </verify>
  <done>
    - load_osmnx_trails function exists in routing_2026.py
    - Function uses osmnx.graph_from_bbox with custom_filter for hiking highway types
    - Function projects graph using ox.project_graph
    - Nodes extracted with x,y coordinates and added to RoutingNetwork
    - Edges added bidirectional with length weight
    - EPSG set on returned RoutingNetwork
  </done>
  <acceptance_criteria>
    - routing_2026.py contains "def load_osmnx_trails(bbox, epsg=25832)"
    - routing_2026.py contains "import osmnx"
    - routing_2026.py contains '["highway"~"path|footway|track|steps"]'
    - routing_2026.py contains "ox.graph_from_bbox"
    - routing_2026.py contains "ox.project_graph"
    - routing_2026.py contains "routing_net.add_node"
    - routing_2026.py contains "routing_net.add_edge"
    - Command: `python -c "import routing_2026; print(hasattr(routing_2026, 'load_osmnx_trails'))"` returns True
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for OSM integration</name>
  <files>tests/test_osmnx_integration.py</files>
  <read_first>
    routing_2026.py
    .planning/phases/02-routing-network-construction/tests/conftest.py
    tests/test_routing_graph.py
  </read_first>
  <action>
    Create tests/test_osmnx_integration.py with unit tests for load_osmnx_trails:

    1. **Imports**: import routing_2026, import pytest, import osmnx as ox (for mocking)

    2. **Mock osmnx graph fixture** (if not in conftest):
       - Create fixture mock_osmnx_graph() that returns fake osmnx MultiDiGraph
       - Nodes: 3 nodes with x=450000, y=6500000 (UTM 32V coords)
       - Edges: 2 edges with length=100, highway='path'
       - Use pytest-mock or unittest.mock if needed

    3. **Test functions**:
       - `test_load_osmnx_trails_mock`: Mock ox.graph_from_bbox and ox.project_graph, verify function calls correct API with custom_filter
       - `test_load_osmnx_trails_node_extraction`: Use mock graph, verify all nodes converted with x,y coordinates
       - `test_load_osmnx_trails_edge_extraction`: Use mock graph, verify edges added bidirectional with weight=length
       - `test_load_osmnx_trails_epsg_set`: Verify returned RoutingNetwork has epsg=25832
       - `test_load_osmnx_trails_edge_attributes`: Verify edge attributes include source='osm' and highway type

    4. **Integration test (optional, marked with pytest.mark.slow)**:
       - `test_load_osmnx_trails_real`: Load actual OSM data for small Norway bbox, verify non-empty graph (this may be slow or fail offline)

    Strategy: Use pytest-mock's monkeypatch or unittest.mock.patch to mock osmnx API calls. This avoids network dependency and OSM data changes during testing.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_osmnx_integration.py -x -v -m "not slow"</automated>
  </verify>
  <done>
    - test_osmnx_integration.py exists with 5+ test functions
    - Mock tests verify correct osmnx API usage (graph_from_bbox with custom_filter)
    - Node extraction tested with mock graph
    - Edge extraction tested with bidirectional edges
    - EPSG propagation verified
    - Edge attributes (source='osm', highway type) verified
  </done>
  <acceptance_criteria>
    - File "tests/test_osmnx_integration.py" exists
    - test_osmnx_integration.py contains "def test_load_osmnx_trails_mock"
    - test_osmnx_integration.py contains "def test_load_osmnx_trails_node_extraction"
    - test_osmnx_integration.py contains "def test_load_osmnx_trails_edge_extraction"
    - test_osmnx_integration.py contains "def test_load_osmnx_trails_epsg_set"
    - test_osmnx_integration.py contains "from unittest.mock import patch" or "monkeypatch"
    - Command: `pytest tests/test_osmnx_integration.py -v -m "not slow"` exits 0 with 4+ tests passing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| osmnx → OpenStreetMap API | Network request to external OSM server |
| OSM data → RoutingNetwork | Untrusted OSM node IDs and coordinates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-07 | Denial of Service | Large OSM bounding box queries | mitigate | For this task: No bbox validation. Document that bbox size should be limited to ~100km x 100km for performance. Future enhancement: add bbox area check and reject >10000km² |
| T-2-08 | Tampering | Malicious OSM data (poisoned coordinates) | mitigate | OSM data is signed/trusted, but for robustness: validate x/y coordinates are finite numbers. Add NaN/inf check in add_node: use numpy.isfinite(). For Wave 0: document as known limitation |
| T-2-09 | Elevation of Privilege | osmnx code execution in graph_from_bbox | accept | Accepted risk: osmnx is well-maintained library from PyPI. No user input passed to eval/exec. Risk is minimal |
</threat_model>

<verification>
OSM integration coverage:
- pytest tests/test_osmnx_integration.py -v -m "not slow" should show 4+ passing tests
- Integration test (for CI/fast runs): test_load_osmnx_trails_mock verifies correct API usage
- Node extraction: test_load_osmnx_trails_node_extraction confirms x,y coordinates preserved
- Edge extraction: test_load_osmnx_trails_edge_extraction confirms bidirectional edges with length weight
</verification>

<success_criteria>
OSM trail loading functional:
- load_osmnx_trails function loads OSM hiking trails
- Correct highway filter applied (path, footway, track, steps)
- Graph projected to metric EPSG (25832)
- Nodes and edges converted to RoutingNetwork format
- Edge metadata preserved (source='osm', highway type)
- All unit tests pass with mocks
- code follows osmnx API patterns from RESEARCH.md
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing-network-construction/02-03-SUMMARY.md`
</output>