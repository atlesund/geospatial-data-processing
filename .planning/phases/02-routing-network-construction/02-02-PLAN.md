---
phase: 02-routing-network-construction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [routing_2026.py]
autonomous: true
requirements: [COMP-03, COMP-04, COMP-05]

must_haves:
  truths:
    - RoutingNetwork class wraps networkx.Graph for geospatial routing
    - RoutingNetwork stores node coordinates in node_coords dictionary
    - RoutingNetwork methods add_node, add_edge, shortest_path work correctly
    - RoutingNetwork supports EPSG tracking for coordinate system validation
    - find_nearest_node uses scipy KDTree for efficient O(log n) lookup
  artifacts:
    - path: "routing_2026.py"
      provides: "RoutingNetwork class wrapper for graph operations"
      exports: ["RoutingNetwork"]
      min_lines: 100
  key_links:
    - from: "routing_2026.py"
      to: "networkx.Graph"
      via: "self.graph = nx.Graph()"
      pattern: "import networkx"
    - from: "RoutingNetwork.add_node"
      to: "node_coords dict"
      via: "self.node_coords[node_id] = (x, y)"
      pattern: "node_coords\["
    - from: "RoutingNetwork.find_nearest_node"
      to: "scipy.spatial.KDTree"
      via: "tree = scipy.spatial.KDTree"
      pattern: "KDTree"
---

<objective>
Create RoutingNetwork class wrapper around networkx.Graph with geospatial methods for routing operations.

Purpose: Provide a clean abstraction layer for graph operations that stores node coordinates, edge weights, and EPSG metadata. This class will be used by all subsequent plans for OSM integration (COMP-04), trail conversion (COMP-03), and terrain mesh construction (COMP-05).

Output: New routing_2026.py file with RoutingNetwork class implementing add_node, add_edge, shortest_path, and find_nearest_node methods
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RoutingNetwork class core structure</name>
  <files>routing_2026.py</files>
  <read_first>
    .planning/phases/02-routing-network-construction/02-RESEARCH.md
    geo_2026.py
  </read_first>
  <behavior>
    - Test 1: RoutingNetwork.__init__ creates empty graph and node_coords dict
    - Test 2: add_node(node_id, x, y) adds node to graph and stores coordinates in node_coords
    - Test 3: add_edge(u, v, weight, **attrs) adds bidirectional edge with weight attribute
    - Test 4: shortest_path(source, target) returns node list using Dijkstra algorithm
    - Test 5: find_nearest_node(x, y) returns nearest node_id and distance using KDTree
  </behavior>
  <action>
    Create routing_2026.py with the RoutingNetwork class:

    1. **Imports**: import networkx as nx, import scipy.spatial, import numpy as np

    2. **RoutingNetwork.__init__(self)**:
       - Create self.graph = nx.Graph()
       - Create self.node_coords = {}  # node_id -> (x, y) tuple
       - Create self.epsg = None  # EPSG code for coordinate system

    3. **add_node(self, node_id, x, y)**:
       - Add node_id to self.graph: self.graph.add_node(node_id)
       - Store coordinates: self.node_coords[node_id] = (x, y)
       - Return None

    4. **add_edge(self, u, v, weight, **attrs)**:
       - Add bidirectional edge: self.graph.add_edge(u, v, weight=weight, **attrs)
       - Ensure weight attribute is present (required for shortest path)
       - Return None

    5. **shortest_path(self, source, target)**:
       - Call nx.dijkstra_path with weight parameter
       - Return list of node IDs forming the path
       - Raise NetworkXNoPath if no path exists (let exception propagate)

    6. **find_nearest_node(self, x, y, k=1)**:
       - Convert node_coords values to numpy array
       - Create scipy.spatial.KDTree with coordinates
       - Query KDTree for k nearest neighbors to [x, y]
       - Return (node_id, distance) tuple for nearest node
       - Handle empty graph case: return (None, float('inf'))

    Follow the naming and code style patterns from geo_2026.py and vector_2026.py:
    - Use 4-space indentation
    - Private attributes with leading underscore (only if needed internally)
    - Clear docstrings for public methods
  </action>
  <verify>
    <automated>pytest tests/test_routing_graph.py::test_routing_network_core -x -v</automated>
  </verify>
  <done>
    - routing_2026.py exists with RoutingNetwork class
    - __init__ creates nx.Graph and node_coords dict
    - add_node stores coordinates correctly
    - add_edge adds bidirectional edges with weight
    - shortest_path returns Dijkstra path
    - find_nearest_node uses KDTree for efficient lookup
  </done>
  <acceptance_criteria>
    - File "routing_2026.py" exists
    - routing_2026.py contains "class RoutingNetwork"
    - routing_2026.py contains "def __init__(self)"
    - routing_2026.py contains "def add_node(self, node_id, x, y)"
    - routing_2026.py contains "def add_edge(self, u, v, weight, **attrs)"
    - routing_2026.py contains "def shortest_path(self, source, target)"
    - routing_2026.py contains "def find_nearest_node(self, x, y, k=1)"
    - routing_2026.py contains "import networkx"
    - routing_2026.py contains "import scipy.spatial"
    - Command: `python -c "import routing_2026; print(routing_2026.RoutingNetwork)"` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add EPSG property and validation to RoutingNetwork</name>
  <files>routing_2026.py</files>
  <read_first>routing_2026.py</read_first>
  <action>
    Extend RoutingNetwork class with EPSG tracking:

    1. **Add EPSG property** following the pattern from vector_2026.py:
       - Create _get_epsg(self) method that returns self._epsg
       - Create _set_epsg(self, epsg_code) method that validates and sets self._epsg
       - Create property: epsg = property(fget=_get_epsg, fset=_set_epsg)

    2. **Validation in _set_epsg**:
       - Accept None (unspecified coordinate system)
       - Accept integer EPSG codes (e.g., 25832 for UTM 32V, 4326 for WGS84)
       - Raise ValueError if epsg_code is not None/int

    3. **Change self.epsg to self._epsg in __init__**:
       - Change self.epsg = None to self._epsg = None

    Follow the exact property pattern from vector_2026.py:
    ```python
    def _get_epsg(self):
        return self._epsg

    def _set_epsg(self, epsg_code):
        self._epsg = epsg_code

    epsg = property(fget=_get_epsg, fset=_set_epsg)
    ```
  </action>
  <verify>
    <automated>pytest tests/test_routing_graph.py::test_epsg_property -x -v</automated>
  </verify>
  <done>
    - EPSG property uses _epsg private attribute
    - _get_epsg returns self._epsg
    - _set_epsg validates type (int or None)
    - Setting invalid EPSG raises ValueError
  </done>
  <acceptance_criteria>
    - routing_2026.py contains "def _get_epsg(self)"
    - routing_2026.py contains "def _set_epsg(self, epsg_code)"
    - routing_2026.py contains "epsg = property(fget=_get_epsg, fset=_set_epsg)"
    - routing_2026.py contains "self._epsg = None"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for RoutingNetwork</name>
  <files>tests/test_routing_graph.py</files>
  <read_first>
    routing_2026.py
    .planning/phases/02-routing-network-construction/tests/conftest.py
    .planning/phases/01-map-interaction-user-selection/tests/test_route_selector.py
  </read_first>
  <action>
    Create tests/test_routing_graph.py with unit tests for RoutingNetwork:

    1. **Imports**: import routing_2026, import pytest

    2. **Test functions**:
       - `test_routing_network_init`: Verify routing_net.graph and node_coords are empty after init
       - `test_add_node`: Add a node and verify it appears in graph and node_coords
       - `test_add_edge`: Add edge and verify it's bidirectional with weight attribute
       - `test_shortest_path`: Create 3-node graph with 2 edges, verify shortest path returns correct sequence
       - `test_find_nearest_node`: Add nodes at known coordinates, query near point, verify nearest returned
       - `test_epsg_property`: Set EPSG to 25832, read back, verify value matches
       - `test_find_nearest_node_empty_graph`: Query nearest on empty graph, verify returns (None, inf)

    3. **Use pytest markers**: Use @pytest.mark.routing marker on test functions

    Follow the test structure from Phase 1 test_route_selector.py:
    - Use pytest fixtures where appropriate
    - Clear test names describing what's being tested
    - Minimal setup/teardown needed

    File location: tests/test_routing_graph.py (root tests directory, not inside phase directory)
  </action>
  <verify>
    <automated>pytest tests/test_routing_graph.py -x -v</automated>
  </verify>
  <done>
    - All 7 unit tests pass
    - RoutingNetwork methods tested individually
    - EPSG property validated
    - Empty graph edge case handled in find_nearest_node
  </done>
  <acceptance_criteria>
    - File "tests/test_routing_graph.py" exists
    - test_routing_graph.py contains "def test_routing_network_init"
    - test_routing_graph.py contains "def test_add_node"
    - test_routing_graph.py contains "def test_add_edge"
    - test_routing_graph.py contains "def test_shortest_path"
    - test_routing_graph.py contains "def test_find_nearest_node"
    - test_routing_graph.py contains "def test_epsg_property"
    - test_routing_graph.py contains "def test_find_nearest_node_empty_graph"
    - Command: `pytest tests/test_routing_graph.py -v` exits 0 with 7+ tests passing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External → RoutingNetwork.add_node | Node coordinates from untrusted sources |
| RoutingNetwork → NetworkX | Graph operations with large data structures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-04 | Denial of Service | Graph size limits in RoutingNetwork | mitigate | No explicit limits in this task. Add max_nodes parameter to __init__ with default None, raise ValueError if exceeded. Document for future enhancement |
| T-2-05 | Tampering | Edge weight validation in add_edge | mitigate | Add check for numeric weight (int/float) and non-negative. For Wave 0: document this as known limitation |
| T-2-06 | Information Disclosure | Node coordinate storage | accept | Coordinates are routing graph topology data, not sensitive information. Accept risk |
</threat_model>

<verification>
Unit tests cover all public methods:
- pytest tests/test_routing_graph.py -v should show 7 passing tests
- All methods (add_node, add_edge, shortest_path, find_nearest_node, epsg property) have dedicated test functions
</verification>

<success_criteria>
RoutingNetwork class fully functional:
- Class wraps networkx.Graph correctly
- All public methods work as expected (add_node, add_edge, shortest_path, find_nearest_node)
- EPSG property validated
- All 7 unit tests pass
- code follows project style patterns (4-space indent, property decorators)
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing-network-construction/02-02-SUMMARY.md`
</output>