---
phase: 09
plan: 04
type: execute
wave: 4
depends_on: [09-01, 09-02, 09-03]
files_modified: [tests/test_09_water_crossing_performance.py]
autonomous: true
requirements: [COMP-01]
user_setup: []
must_haves:
  truths:
    - "Indexed version produces identical results to naive iteration"
    - "Performance test completes in <10 seconds for typical dataset"
    - "All Phase 4 water routing tests still pass after optimization"
    - "Functional equivalence proven through edge-by-edge comparison"
  artifacts:
    - path: "tests/test_09_water_crossing_performance.py"
      provides: "Performance and equivalence test suite"
      min_lines: 150
  key_links:
    - from: "test_09_water_crossing_performance.py"
      to: "routing_2026.py"
      via: "Import and test spatial index functions"
      pattern: "import routing_2026"
---

<objective>
Validate spatial index optimization through performance testing and functional equivalence verification.

Purpose: Prove that the indexed water crossing detection produces identical results to the naive implementation while achieving significant performance improvement. Ensure backward compatibility with existing Phase 4 water routing functionality.
Output: Comprehensive test suite (test_09_water_crossing_performance.py) that verifies functional equivalence and measures performance improvement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-optimize-water-crossing-detection-with-spatial-indexing/09-RESEARCH.md
@routing_2026.py

## Validation Requirements

### 1. Functional Equivalence Test

**Goal:** Prove indexed version produces identical results to naive version

**Method:**
1. Create small dataset (100 lakes, 50 rivers, 1000 edges)
2. Create naive version function for comparison
3. Run both versions on same edges
4. Compare all penalty results (must be identical)

**Test approach:**
```python
for edge in test_edges:
    penalty_naive = detect_water_crossing_naive(...)
    penalty_indexed = detect_water_crossing_indexed(...)
    assert penalty_naive == penalty_indexed
```

### 2. Performance Test

**Target:** Completes in <10 seconds for typical dataset

**Profiling:**
```python
import time
start = time.time()
# Run with 500k edges, 30k lakes, 20k rivers
end = time.time()
print(f"Total time: {end - start}s")
```

**Expected performance:**
- Naive version: Hours/days (system hangs)
- Indexed version: <10 seconds
- Speedup: >1000×

### 3. Backward Compatibility Test

**Verify:**
- All Phase 4 water routing tests still pass
- terrain_mesh_from_raster works with enable_water_queries=False
- Edge weights contain correct penalty factors (terrain × water)
- Fjord detection still works
- Lake and river detection still works

## Test Data Requirements

### Synthetic Test Data

Create small synthetic water features for functional equivalence testing:

```python
# Create 100 small lakes (circles) in grid pattern
lakes = []
for i in range(10):
    for j in range(10):
        center = Point(i * 10, j * 10)
        lakes.append({
            'name': f'Lake_{i}_{j}',
            'geometry': center.buffer(2)  # Circle radius 2
        })

# Create 50 short rivers (lines) crossing grid
rivers = []
for i in range(10):
    for j in range(5):
        river = LineString([(i * 10, j * 20), (i * 10, j * 20 + 15)])
        rivers.append({
            'name': f'River_{i}_{j}',
            'geometry': river
        })
```

### Edge Test Pattern

Create edges that cross various scenarios:
- Edges inside lakes (should detect lake)
- Edges crossing rivers (should detect river)
- Edges across both lake and river (priority order)
- Edges in open water (no detection)
- Edges near fjords (should detect fjord when name contains 'fjord')

## Performance Benchmarks

### Expected Performance Metrics

| Dataset Size | Naive Time (estimated) | Indexed Time | Speedup |
|--------------|----------------------|--------------|---------|
| 100 lakes, 50 rivers, 1000 edges | ~2 seconds | ~0.1 seconds | 20× |
| 1000 lakes, 500 rivers, 10k edges | ~200 seconds | ~2 seconds | 100× |
| 30k lakes, 20k rivers, 500k edges | ~10,000 seconds | ~10 seconds | 1000× |

### Test Execution Time Budget

- Functional equivalence test: <30 seconds
- Performance test (small dataset): <10 seconds
- Integration test with raster: <60 seconds
- Total test suite: <2 minutes

## Implementation Notes

### Naive Version Implementation

Create temporary naive version for comparison:
```python
def detect_water_crossing_naive(edge_start, edge_end, lakes_gdf, rivers_gdf,
                                lake_penalty=10.0, river_penalty=5.0, fjord_penalty=50.0):
    """Naive O(n×m) version for equivalence testing."""
    # Implementation identical to original detect_water_crossing (pre-Phase 9)
```

### Test Organization

Structure test file as:
1. Synthetic data generation fixtures
2. Naive version implementation (for comparison only)
3. Functional equivalence tests
4. Performance tests
5. Integration tests
6. Edge case tests (empty data, None inputs, corner cases)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive test suite for spatial index validation</name>
  <files>tests/test_09_water_crossing_performance.py</files>
  <read_first>routing_2026.py</read_first>
  <action>
Create test file tests/test_09_water_crossing_performance.py with:

```python
"""
Test suite for Phase 9: Spatial index optimization for water crossing detection.

Validates:
1. Functional equivalence: indexed version produces identical results to naive
2. Performance: indexed version completes in reasonable time
3. Backward compatibility: existing Phase 4 tests still pass
"""

import pytest
import time
import geopandas as gpd
from shapely.geometry import Point, LineString, Polygon
import numpy as np
import routing_2026 as routing


def generate_synthetic_water_data(num_lakes=10, num_rivers=5, grid_size=10):
    """
    Generate synthetic water feature dataset for testing.

    Args:
        num_lakes: Number of lakes to create per row/column
        num_rivers: Number of rivers to create
        grid_size: Spacing between water features

    Returns:
        Tuple (lakes_gdf, rivers_gdf) - GeoDataFrames with synthetic water features
    """
    lakes = []
    for i in range(num_lakes):
        for j in range(num_lakes):
            center = Point(i * grid_size, j * grid_size)
            lakes.append({
                'name': f'Lake_{i}_{j}',
                'geometry': center.buffer(grid_size / 4)  # Circle radius
            })

    rivers = []
    for i in range(num_rivers):
        # Vertical rivers
        river = LineString([(i * grid_size, -grid_size), (i * grid_size, grid_size * num_rivers)])
        rivers.append({
            'name': f'River_{i}',
            'geometry': river
        })

    lakes_gdf = gpd.GeoDataFrame(lakes)
    rivers_gdf = gpd.GeoDataFrame(rivers)

    return lakes_gdf, rivers_gdf


def detect_water_crossing_naive(edge_start, edge_end, lakes_gdf, rivers_gdf,
                                lake_penalty=10.0, river_penalty=5.0, fjord_penalty=50.0):
    """
    Naive O(n×m) implementation of water crossing detection.

    This is the original implementation (pre-Phase 9) used for
    functional equivalence testing. Do NOT use in production.
    """
    if lakes_gdf is None and rivers_gdf is None:
        return (None, 1.0)

    x1, y1 = edge_start
    x2, y2 = edge_end
    midpoint = Point(((x1 + x2) / 2, (y1 + y2) / 2))

    # Check lakes (naive iteration)
    if lakes_gdf is not None:
        for idx, lake_row in lakes_gdf.iterrows():
            lake_geom = lake_row.geometry
            if midpoint.within(lake_geom):
                name = lake_row.get('name', '')
                if name and 'fjord' in str(name).lower():
                    return ('fjord', fjord_penalty)
                return ('lake', lake_penalty)

    # Check rivers (naive iteration)
    if rivers_gdf is not None:
        edge_line = LineString([edge_start, edge_end])
        for idx, river_row in rivers_gdf.iterrows():
            river_geom = river_row.geometry
            if edge_line.intersects(river_geom):
                return ('river', river_penalty)

    return (None, 1.0)


class TestFunctionalEquivalence:
    """Tests verifying indexed version produces identical results to naive version."""

    def test_equivalence_small_dataset(self):
        """Test functional equivalence with small dataset."""
        lakes_gdf, rivers_gdf = generate_synthetic_water_data(num_lakes=5, num_rivers=3)
        lake_tree, river_tree = routing.build_spatial_indexes(lakes_gdf, rivers_gdf)

        # Generate test edges covering various scenarios
        test_edges = [
            ((2.5, 2.5), (2.6, 2.6)),  # Inside lake
            ((0, -5), (5, 15)),  # Crosses river
            ((10, 10), (11, 11)),  # Open space
            ((2.5, 2.5), (3.5, 3.5)),  # Partial lake
            ((-1, -1), (1, 1)),  # Crosses lake and river
        ]

        for edge in test_edges:
            naive_result = detect_water_crossing_naive(
                edge[0], edge[1], lakes_gdf, rivers_gdf
            )
            indexed_result = routing.detect_water_crossing(
                edge[0], edge[1], lake_tree, river_tree,
                lakes_gdf=lakes_gdf, rivers_gdf=rivers_gdf
            )
            assert naive_result == indexed_result, \
                f"Edge {edge}: naive={naive_result}, indexed={indexed_result}"

    def test_equivalence_fjord_detection(self):
        """Test fjord detection equivalence."""
        # Create fjord
        lakes = [{'name': 'Sognefjorden', 'geometry': Point(0, 0).buffer(5)}]
        lakes_gdf = gpd.GeoDataFrame(lakes)
        river_tree = routing.build_spatial_indexes(lakes_gdf, gpd.GeoDataFrame())

        lake_tree, _ = river_tree

        edge = ((0, 0), (1, 1))
        naive_result = detect_water_crossing_naive(
            edge[0], edge[1], lakes_gdf, gpd.GeoDataFrame()
        )
        indexed_result = routing.detect_water_crossing(
            edge[0], edge[1], lake_tree, None,
            lakes_gdf=lakes_gdf, rivers_gdf=None
        )
        assert naive_result == indexed_result == ('fjord', 50.0)

    def test_equivalence_empty_data(self):
        """Test equivalence with empty GeoDataFrames."""
        empty_lakes = gpd.GeoDataFrame({'geometry': []})
        empty_rivers = gpd.GeoDataFrame({'geometry': []})

        lake_tree, river_tree = routing.build_spatial_indexes(empty_lakes, empty_rivers)
        assert lake_tree is None and river_tree is None

        edge = ((0, 0), (1, 1))
        naive_result = detect_water_crossing_naive(edge[0], edge[1], empty_lakes, empty_rivers)
        indexed_result = routing.detect_water_crossing(edge[0], edge[1], lake_tree, river_tree)
        assert naive_result == indexed_result == (None, 1.0)

    def test_equivalence_none_inputs(self):
        """Test equivalence with None inputs."""
        edge = ((0, 0), (1, 1))
        naive_result = detect_water_crossing_naive(edge[0], edge[1], None, None)
        indexed_result = routing.detect_water_crossing(edge[0], edge[1], None, None)
        assert naive_result == indexed_result == (None, 1.0)


class TestPerformance:
    """Tests for performance validation of spatial index optimization."""

    def test_performance_small_dataset(self):
        """Test performance with small dataset (100 edges)."""
        lakes_gdf, rivers_gdf = generate_synthetic_water_data(num_lakes=10, num_rivers=5)
        lake_tree, river_tree = routing.build_spatial_indexes(lakes_gdf, rivers_gdf)

        # Generate 100 test edges
        test_edges = [
            ((np.random.uniform(0, 50), np.random.uniform(0, 50)),
             (np.random.uniform(0, 50), np.random.uniform(0, 50)))
            for _ in range(100)
        ]

        start = time.time()
        for edge in test_edges:
            _ = routing.detect_water_crossing(edge[0], edge[1], lake_tree, river_tree)
        elapsed = time.time() - start

        assert elapsed < 2.0, f"100 edges took {elapsed:.2f}s (expected <2s)"
        print(f"Performance: 100 edges in {elapsed:.3f}s ({elapsed*1000:.1f}ms/edge)")

    def test_performance_medium_dataset(self):
        """Test performance with medium dataset (1000 edges)."""
        lakes_gdf, rivers_gdf = generate_synthetic_water_data(num_lakes=20, num_rivers=10)
        lake_tree, river_tree = routing.build_spatial_indexes(lakes_gdf, rivers_gdf)

        # Generate 1000 test edges
        test_edges = [
            ((np.random.uniform(0, 100), np.random.uniform(0, 100)),
             (np.random.uniform(0, 100), np.random.uniform(0, 100)))
            for _ in range(1000)
        ]

        start = time.time()
        for edge in test_edges:
            _ = routing.detect_water_crossing(edge[0], edge[1], lake_tree, river_tree)
        elapsed = time.time() - start

        assert elapsed < 5.0, f"1000 edges took {elapsed:.2f}s (expected <5s)"
        print(f"Performance: 1000 edges in {elapsed:.3f}s ({elapsed*1000:.1f}ms/edge)")

    def test_build_index_performance(self):
        """Test spatial index build performance."""
        lakes_gdf, rivers_gdf = generate_synthetic_water_data(num_lakes=100, num_rivers=50)

        start = time.time()
        lake_tree, river_tree = routing.build_spatial_indexes(lakes_gdf, rivers_gdf)
        elapsed = time.time() - start

        assert elapsed < 2.0, f"Index build took {elapsed:.2f}s (expected <2s)"
        assert lake_tree is not None and river_tree is not None
        print(f"Index build: {len(lakes_gdf)} lakes, {len(rivers_gdf)} rivers in {elapsed:.3f}s")


class TestBackwardCompatibility:
    """Tests for backward compatibility with existing Phase 4 functionality."""

    def test_mesh_generation_no_water_queries(self):
        """Test mesh generation with water queries disabled."""
        # This tests backward compatibility (enable_water_queries=False)
        # Uses synthetic raster if available, otherwise skips
        try:
            from raster_2026 import Raster
            import os

            test_tif = '/Users/dev/Code/School/geospatial-data-processing/data/dtm_50_1000.tif'
            if os.path.exists(test_tif):
                test_raster = Raster()
                test_raster.read_image(test_tif)

                mesh_net = routing.terrain_mesh_from_raster(
                    test_raster, mesh_spacing=50, enable_water_queries=False
                )

                assert mesh_net is not None
                assert len(mesh_net.node_coords) > 0
                assert len(mesh_net.graph.edges) > 0

                # Verify edge attributes exist
                edge_data = list(mesh_net.graph.edges(data=True))[0]
                assert 'water_type' in edge_data[2]
                assert 'water_penalty_factor' in edge_data[2]
        except ImportError:
            pytest.skip("Raster module not available")
        except FileNotFoundError:
            pytest.skip("Test raster not found")

    def test_edge_penalty_factors(self):
        """Test that edge penalty factors are calculated correctly."""
        lakes_gdf, rivers_gdf = generate_synthetic_water_data(num_lakes=3, num_rivers=2)
        lake_tree, river_tree = routing.build_spatial_indexes(lakes_gdf, rivers_gdf)

        # Edge inside lake (10× penalty)
        result = routing.detect_water_crossing((2.5, 2.5), (2.6, 2.6), lake_tree, river_tree, lakes_gdf=lakes_gdf)
        assert result[0] == 'lake' and result[1] == 10.0

        # Edge crossing river (5× penalty)
        result = routing.detect_water_crossing((0, -5), (5, 15), lake_tree, river_tree)
        assert result[0] == 'river' and result[1] == 5.0

        # Edge in open space (1× penalty)
        result = routing.detect_water_crossing((20, 20), (21, 21), lake_tree, river_tree)
        assert result == (None, 1.0)


class TestEdgeCases:
    """Tests for edge cases and error conditions."""

    def test_empty_lakes_valid_rivers(self):
        """Test with empty lakes but valid rivers."""
        empty_lakes = gpd.GeoDataFrame({'geometry': []})
        rivers = [{'name': 'River', 'geometry': LineString([(0, -5), (0, 5)])}]
        rivers_gdf = gpd.GeoDataFrame(rivers)

        lake_tree, river_tree = routing.build_spatial_indexes(empty_lakes, rivers_gdf)
        assert lake_tree is None
        assert river_tree is not None

        result = routing.detect_water_crossing(
            (-1, -1), (1, 1), lake_tree, river_tree
        )
        assert result[0] == 'river'

    def test_valid_lakes_empty_rivers(self):
        """Test with valid lakes but empty rivers."""
        lakes = [{'name': 'Lake', 'geometry': Point(0, 0).buffer(5)}]
        lakes_gdf = gpd.GeoDataFrame(lakes)
        empty_rivers = gpd.GeoDataFrame({'geometry': []})

        lake_tree, river_tree = routing.build_spatial_indexes(lakes_gdf, empty_rivers)
        assert lake_tree is not None
        assert river_tree is None

        result = routing.detect_water_crossing(
            (0, 0), (1, 1), lake_tree, river_tree,
            lakes_gdf=lakes_gdf
        )
        assert result[0] == 'lake'

    def test_edge_exactly_on_river_line(self):
        """Test edge that exactly coincides with river line."""
        rivers = [{'name': 'River', 'geometry': LineString([(0, 0), (10, 10)])}]
        rivers_gdf = gpd.GeoDataFrame(rivers)
        lake_tree, river_tree = routing.build_spatial_indexes(
            gpd.GeoDataFrame({'geometry': []}), rivers_gdf
        )

        # Edge exactly on river line
        result = routing.detect_water_crossing((5, 5), (6, 6), lake_tree, river_tree)
        assert result[0] == 'river'


if __name__ == '__main__':
    # Run tests with pytest if available, otherwise skip
    try:
        pytest.main([__file__, '-v', '--tb=short'])
    except:
        print("pytest not available, skipping automated test run")
```
  </action>
  <verify>
    <automated>cd /Users/dev/Code/School/geospatial-data-processing && python tests/test_09_water_crossing_performance.py</automated>
  </verify>
  <done>
    - test_09_water_crossing_performance.py file created with comprehensive test suite
    - Functional equivalence tests verify indexed and naive versions produce identical results
    - Performance tests confirm indexed version completes in expected time
    - Backward compatibility tests ensure Phase 4 functionality still works
    - Edge case tests cover empty data, None inputs, and corner conditions
    - All tests pass successfully
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test data generation → test execution | Synthetic data for validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-19 | Spoofing | Test data | accept | Synthetic data is trusted source |
| T-09-20 | Tampering | Test results | accept | In-memory validation |
| T-09-21 | Repudiation | None | accept | No audit requirement for tests |
| T-09-22 | Information Disclosure | None | accept | No sensitive data in tests |
| T-09-23 | Denial of Service | Performance tests | mitigate | Time limits on tests, small dataset sizes |
| T-09-24 | Elevation of Privilege | None | accept | No privilege boundaries in tests |

**Mitigation Details:**
- T-09-23: Performance tests use small datasets (100-1000 edges) and enforce time limits. Tests skip if test raster not found.
</threat_model>

<verification>
1. Verify test file exists and is structured correctly
2. Run functional equivalence tests - all must pass
3. Run performance tests - all must complete within time limits
4. Run backward compatibility tests - all must pass
5. Run edge case tests - all must pass
6. Verify total test execution time <2 minutes
7. Verify no test failures or errors
</verification>

<success_criteria>
1. Test file tests/test_09_water_crossing_performance.py created
2. All functional equivalence tests pass (indexed == naive)
3. All performance tests complete within time limits (<2s for 100 edges, <5s for 1000 edges)
4. All backward compatibility tests pass
5. All edge case tests pass
6. Total test suite execution time <2 minutes
7. Phase 4 water routing tests still pass (if they exist)
</success_criteria>

<output>
After completion, create `.planning/phases/09-optimize-water-crossing-detection-with-spatial-indexing/09-04-SUMMARY.md`
</output>