---
phase: 8
plan: 04
type: execute
wave: 1
depends_on: [08-03]
files_modified: []
autonomous: false
requirements:
  - D-06
user_setup: []

must_haves:
  truths:
    - "test_water_query_debug.py executes without timeout errors"
    - "Console output shows tiled query messages for all 4 tiles"
    - "Console output shows merged water feature counts"
    - "Both tests complete successfully (baseline and with water queries)"
  artifacts:
    - path: "test_water_query_debug.py"
      provides: "Validation test script for tiled water query"
      min_lines: 1
      contains: "test_water_query_debug.py"
  key_links:
    - from: "test_water_query_debug.py"
      to: "routing_2026.py:terrain_mesh_from_raster"
      via: "function call during test execution"
      pattern: "terrain_mesh_from_raster.*enable_water_queries"
---

<objective>
Test and validate the tiled water query implementation by running the existing test script and confirming successful water feature loading over full raster areas.

Purpose: Verify that the tiled approach fixes the OSM API timeout issue and successfully loads water features for entire raster areas without errors.
Output: Validation test results confirming tiled implementation works correctly
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-fix-osm-api-integration-for-querying-water-features-and-hiki/08-CONTEXT.md
@.planning/debug/water-query-bbox-issue.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Run water query test to validate tiled implementation</name>
  <files>test_water_query_debug.py</files>
  <action>Execute the test_water_query_debug.py script to validate that the tiled water query implementation works correctly.

The script performs:
1. Load real GeoTIFF raster from /data/terrain/6701_50m_33.tif
2. Test baseline: terrain_mesh_from_raster with enable_water_queries=False (should pass)
3. Test water queries: terrain_mesh_from_raster with enable_water_queries=True (should now pass with tiled approach)

Expected results:
- Test 1 (no water queries): Passes as before
- Test 2 (water queries enabled): Should now complete successfully
- Console output should show "Querying water features using tiled approach (2x2 grid)..."
- Console output should show "Querying tile 1/4...", "Querying tile 2/4...", "Querying tile 3/4...", "Querying tile 4/4..."
- Console output should show water feature counts (e.g., "Query complete: X lakes, Y rivers found")

Run the test:

```bash
cd /Users/dev/Code/School/geospatial-data-processing
python test_water_query_debug.py
```

**Note:** This task uses the existing test_water_query_debug.py script for validation - no new test code is created. The test script was pre-existing from Phase 4 debugging effort.</action>
  <verify>echo "Test execution status checked manually by human operator"</verify>
  <done>test_water_query_debug.py executes successfully without timeout errors, showing all 4 tile queries and merged water feature counts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test execution | Running test script that calls terrain_mesh_from_raster |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | R | Test execution | accept | Test-only task, no code modifications. Manual verification required. |
</threat_model>

<verification>
Verification performed by human operator:
1. Confirm test executes without timeout errors
2. Check console output shows "Querying water features using tiled approach (2x2 grid)..."
3. Verify all 4 tiles queried ("Querying tile 4/4..." appears)
4. Confirm merged water feature counts displayed ("Query complete: X lakes, Y rivers found")
5. Validate both tests complete successfully

**Note:** This uses the pre-existing test_water_query_debug.py script for validation. No new automated test creation required.
</verification>

<success_criteria>
- test_water_query_debug.py executes without timeout errors when enable_water_queries=True
- Console output shows "Querying water features using tiled approach (2x2 grid)..."
- Console output shows all 4 tile queries completed ("Querying tile 1/4..." through "4/4...")
- Console output shows final merged water feature counts ("Query complete: X lakes, Y rivers found")
- Both Test 1 (no water queries) and Test 2 (water queries enabled) complete successfully
- Water features successfully loaded for full raster area (182km × 108km) without timeout
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-osm-api-integration-for-querying-water-features-and-hiki/08-04-SUMMARY.md`
</output>